diff --git a/brainstorming/SKILL.md b/brainstorming/SKILL.md
index a884603..5c8408a 100644
--- a/brainstorming/SKILL.md
+++ b/brainstorming/SKILL.md
@@ -31,7 +31,7 @@ metadata:
 3. **提出澄清问题** — 每次一个，了解目的/约束/成功标准
 4. **提出 2-3 种方案** — 附带权衡分析和你的推荐
 5. **展示设计** — 按复杂度分节展示，每节展示后获得用户批准
-6. **编写设计文档** — 保存到 `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md` 并 commit
+6. **按需编写设计文档** — 用户要求、项目惯例或已批准流程需要落盘时，保存到约定位置；只有用户明确授权时才 commit
 7. **规格自检** — 快速内联检查占位符、矛盾、模糊性、范围（详见下方）
 8. **用户审查书面规格** — 在继续之前请用户审查规格文件
 9. **过渡到实现** — 调用 writing-plans 技能创建实现计划
@@ -108,6 +108,24 @@ digraph brainstorming {
 
 > 本段提炼自 icode-skill `references/anti_laziness.md` 第 27 条 + `steps/00_init.md` §5「前提假设实验表」，泛化去除 icode 专属的 `00_init.md §5` / `plan §9` 字段依赖，保留"识别外部前提 -> 代码能验证则标注验证方法、不能则设计实验 -> 证伪回写"核心规则。
 
+**现有功能覆盖度检查（防重复实现）：**
+
+对每个拟新增的功能、接口或模块，在方案设计前检查工程中是否已有等价行为：
+
+1. 用需求的“动词 + 名词”及同义词检索当前仓库及需求涉及的嵌套 Git 根，不只查当前模块；遵循项目忽略规则，不扫描生成物、依赖缓存或无关目录。
+2. 命中后读取完整函数/配置上下文，并追踪关键调用方和消费后的实际行为；文本命中不等于行为已覆盖。
+3. 给出带证据位置的三态结论：
+
+| 判定 | 处理方式 |
+|------|---------|
+| 已覆盖 | 复用现有实现或取消新增，不进入重复设计 |
+| 部分覆盖 | 明确现有边界和本次只需补齐的缺口 |
+| 未覆盖 | 记录检索范围和证据后，正常进入方案设计 |
+
+无法确认覆盖范围时标“待验证”，不得默认按未覆盖继续设计。
+
+> 本段提炼自 icode-skill `references/necessity_check.md`，去除了工单、metadata 和固定行数要求，保留“检索命中 → 行为链核实 → 三态判定”的核心机制。
+
 **探索方案：**
 
 - 提出 2-3 种不同的方案及其权衡
@@ -139,10 +157,10 @@ digraph brainstorming {
 
 **文档：**
 
-- 将验证通过的设计（规格说明）写入 `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`
-  - （用户对规格位置的偏好优先于此默认值）
+- 用户要求、项目惯例或已批准流程需要落盘时，将验证通过的设计写入 `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`
+  - 用户对是否落盘及规格位置的偏好优先于默认值
 - 如果可用，使用 elements-of-style:writing-clearly-and-concisely 技能
-- 将设计文档 commit 到 git
+- 只有用户明确要求或已批准计划明确授权时，才将设计文档 commit 到 Git
 
 **规格自检：**
 编写规格文档后，以全新的视角审视它：
@@ -157,7 +175,7 @@ digraph brainstorming {
 **用户审查关卡：**
 规格自检完成后，请用户在继续之前审查书面规格：
 
-> "规格已编写并 commit 到 `<path>`。请审查一下，如果在我们开始编写实现计划之前你想做任何修改，请告诉我。"
+> "规格已整理到 `<path>`（若未要求落盘，则直接在对话中给出）。请审查一下，如果在我们开始编写实现计划之前你想做任何修改，请告诉我。"
 
 等待用户回复。如果他们要求修改，做出修改并重新运行规格自检。只有在用户批准后才继续。
 
diff --git a/brainstorming/spec-document-reviewer-prompt.md b/brainstorming/spec-document-reviewer-prompt.md
index 7e61864..ccecdf7 100644
--- a/brainstorming/spec-document-reviewer-prompt.md
+++ b/brainstorming/spec-document-reviewer-prompt.md
@@ -7,7 +7,7 @@
 **调度时机：** 规格文档写入 docs/superpowers/specs/ 之后
 
 ```
-Task tool（通用）:
+当前平台的审查子代理（Codex 使用 `spawn_agent`；审查材料自包含时设置 `fork_turns="none"`）：
   description: "审查规格文档"
   prompt: |
     你是一名规格文档审查员。验证此规格是否完整并准备好进行计划编写。
diff --git a/executing-plans/SKILL.md b/executing-plans/SKILL.md
index a2276ec..9c8d513 100644
--- a/executing-plans/SKILL.md
+++ b/executing-plans/SKILL.md
@@ -55,7 +55,7 @@ metadata:
 2. **理解目标** — 重读任务描述，明确完成标准
 3. **执行实现** — 严格按照计划步骤执行（计划已有小步骤）
 4. **运行验证** — 按要求运行测试或检查
-5. **提交变更** — 每完成一个任务提交一次，commit message 引用任务编号
+5. **按授权提交变更** — 只有用户明确要求或计划已明确授权时才提交；否则保留工作区改动供用户审查
 6. **标记为已完成** — 更新 TodoWrite
 
 **每个任务的节奏：**
@@ -77,7 +77,7 @@ $ npm test -- --grep "validation"
   ✓ 拒绝过长用户名 (5ms)
   3 passing
 
-[提交]
+[提交（本计划已获用户授权时）]
 $ git add src/middleware/validate.js tests/validation.test.js
 $ git commit -m "feat: 添加用户输入验证（任务 2/5）"
 
@@ -95,7 +95,7 @@ $ git commit -m "feat: 添加用户输入验证（任务 2/5）"
 1. 读错误信息，定位失败原因
 2. 区分：是实现 bug？还是测试本身有问题？还是计划描述有误？
 3. 实现 bug → 修复并重跑
-4. 测试有问题 → 修复测试，向伙伴说明
+4. 测试有问题 → 先报告测试问题；只有用户明确要求编写/修改测试时才修复测试
 5. 计划有误 → 停下来，向伙伴报告并建议修正
 
 **依赖缺失：**
@@ -167,7 +167,7 @@ $ git commit -m "feat: 添加用户输入验证（任务 2/5）"
 - 先批判性审查计划
 - 严格按照计划步骤执行
 - 不要跳过验证
-- 每个任务单独提交，commit message 引用任务编号
+- 只有用户明确授权时才提交；授权逐任务提交时，commit message 引用任务编号
 - 计划要求时引用相应技能
 - 遇到阻塞时停下来，不要猜测
 - 未经用户明确同意，绝不在 main/master 分支上开始实现
diff --git a/requesting-code-review/SKILL.md b/requesting-code-review/SKILL.md
index f904625..712f2fe 100644
--- a/requesting-code-review/SKILL.md
+++ b/requesting-code-review/SKILL.md
@@ -28,21 +28,38 @@ metadata:
 
 ## 如何请求
 
-**1. 获取 git SHA：**
+### 审查范围与多 Git 根
+
+开始审查前，先从用户需求、计划以及 `git status`/diff 确定全部改动路径。对每个改动路径执行：
+
 ```bash
-BASE_SHA=$(git rev-parse HEAD~1)  # 或 origin/main
+git -C <改动文件所在目录> rev-parse --show-toplevel
+```
+
+- 每个不同结果都是独立 Git 根，分别计算 BASE/HEAD、读取状态和生成 diff。
+- 父仓库 diff 为空不代表嵌套仓库或 submodule 没有改动。
+- 未提交和已暂存改动必须与提交区间一起交给审查者。
+- 不能建立可靠基线的 Git 根标为“待验证”，不得静默漏审。
+
+**1. 确定审查基线：**
+```bash
+# 任务审查：使用任务开始前记录的 BASE_SHA
+# 整分支审查：使用与目标分支的 merge-base
+TARGET_BRANCH=<目标分支，如 master 或 origin/main>
+BASE_SHA=$(git merge-base "$TARGET_BRANCH" HEAD)
 HEAD_SHA=$(git rev-parse HEAD)
 ```
 
+**禁止默认使用 `HEAD~1`**：一个任务可能包含多个提交，`HEAD~1` 会静默漏审更早的改动。未提交改动应额外纳入 `git diff` / `git diff --cached`，不能只审查提交区间。
+
 **2. 派遣代码审查子代理：**
 
-使用 Task 工具，指定 `general-purpose` 类型，填写 `code-reviewer.md` 中的模板
+使用当前平台实际提供的子代理工具，填写 `code-reviewer.md` 中的模板。Codex 使用 `spawn_agent`；任务简报已自包含时使用 `fork_turns="none"`，避免继承无关会话历史。当前会话没有子代理能力时，按下方降级路径执行。
 
 **占位符说明：**
 - `{DESCRIPTION}` - 你刚完成的内容简要说明
 - `{PLAN_OR_REQUIREMENTS}` - 预期功能
-- `{BASE_SHA}` - 起始提交
-- `{HEAD_SHA}` - 结束提交
+- `{REVIEW_SCOPES}` - 每个 Git 根的绝对路径、BASE/HEAD、`git status --short`，以及覆盖 committed/staged/unstaged/untracked 改动的审查包或等价 diff
 
 **3. 处理反馈：**
 - Critical 问题立即修复
@@ -63,8 +80,11 @@ HEAD_SHA=$(git rev-parse HEAD)
 [派遣代码审查子代理]
   DESCRIPTION: 添加了 verifyIndex() 和 repairIndex()，支持 4 种问题类型
   PLAN_OR_REQUIREMENTS: docs/superpowers/plans/deployment-plan.md 中的任务 2
-  BASE_SHA: a7981ec
-  HEAD_SHA: 3df7661
+  REVIEW_SCOPES:
+    - Git 根: /path/to/repo
+      Base/Head: a7981ec..3df7661
+      状态: <git status --short 输出>
+      审查包: /path/to/review-a7981ec..3df7661.diff
 
 [子代理返回]:
   优点：架构清晰，测试真实
@@ -115,19 +135,19 @@ HEAD_SHA=$(git rev-parse HEAD)
 
 **主代理实证 ≠ 自演：** 主代理用 Read/Grep 查到的代码/日志铁证（如"某函数签名确实是 X"）属事实核查，可直接 confirmed 无需对抗。**必须独立对抗的**是判断性结论--无铁证、依赖独立视角的判断，主代理自演无效。
 
-**显式等待 + 超时（防"未等子代理返回就降级"）：** spawn 质疑者后必须等 verdict 返回才能进入裁决。超时阈值默认 120 秒。超时触发重试 1 次（换措辞，并仅在当前接口实际支持时调整执行配置）；二次仍超时或无 verdict -> 进入失败处理链路。区分四种状态：
+**显式等待（防“未等子代理返回就降级”）：** spawn 质疑者后必须等待 verdict 返回才能进入裁决。一次等待调用超时只表示“本次等待未收到新消息”，**不表示原子代理已失败**；先查询原 agent 状态并继续等待，禁止因此重复 spawn 一个仍在运行的任务。区分四种状态：
 
 | 状态 | 含义 |
 |------|------|
-| `sync_ok` | 同步等 verdict，120s 内返回 |
-| `timeout_retry_used` | 触发 1 次超时重试 |
-| `still_failed_after_retry` | 重试后仍超时/无 verdict |
+| `sync_ok` | 原 agent 正常返回 verdict |
+| `wait_continued` | 等待调用超时，但原 agent 仍运行，已继续等待 |
+| `replacement_used` | 原 agent 已明确失败/终止或最终输出不可用，启动 1 次替代任务 |
 | `env_no_spawn` | 环境结构性无 spawn 工具（见下方降级路径） |
 
-**子代理失败处理（重试 2 次，绝不自演）：** 质疑者返回空/截断/无裁决时：
-1. **第 1 次重试**：换措辞（强化"先给裁决再给依据"），子代理类型不变
-2. **第 2 次重试**：在当前接口实际支持的范围内更换执行配置（如可用模型或推理强度）；接口不支持时保持配置不变，仅更换措辞后再 spawn 一次
-3. **重试 2 次仍失败** -> 诚实降级为 `[未验证-子代理对抗失败]`，计入待验证清单，**绝不改由主代理自演裁决**（自演=既当运动员又当裁判，确认偏误倾向确认自己结论）
+**子代理失败处理（最多 1 个替代任务，绝不制造重复并发）：**
+1. 原 agent 仍为 running/pending → 继续等待，不 spawn 替代任务
+2. 原 agent 已明确 failed/cancelled/terminated，或最终输出为空、截断、无裁决 → 换措辞后启动 1 个替代任务；仅使用当前接口实际支持的模型或推理配置
+3. 替代任务仍失败 → 诚实降级为 `[未验证-子代理对抗失败]`，计入待验证清单，绝不改由主代理自演裁决
 
 **环境无 spawn 工具的降级路径：** 当当前会话结构性无子代理 spawn 能力（工具未暴露、调用明确返回不支持，或会话类型不支持子代理）时，**不重试、不自演**，走以下降级路径：
 
@@ -143,7 +163,7 @@ HEAD_SHA=$(git rev-parse HEAD)
 
 **Codex CLI 判定方法**：以当前会话是否实际暴露且可调用 `spawn_agent` 为准；配置文件只能作为排障线索，不能单独判定环境无 spawn 工具。
 
-> 本段提炼自 icode-skill `references/adversarial.md`「显式等待+超时机制」「子代理失败处理」「环境无 spawn 工具场景」三段，泛化去除 icode 专属的 `task_timeout_seconds` metadata 字段、`no_spawn_env` flag、产物文件标记要求，保留四态枚举 + 重试 2 次 + 环境无 spawn 降级核心规则。Codex 以当前会话实际工具能力判定。
+> 本段提炼自 icode-skill 的等待和诚实降级思想，并按 Codex 的实际 agent 生命周期修正：等待超时不等于任务失败，只有明确失败后才允许启动替代任务。
 
 ## 多层审查增强（可选）
 
@@ -168,7 +188,7 @@ HEAD_SHA=$(git rev-parse HEAD)
 | 3 | 场景覆盖度 | 空入参/超长输入/并发/重入/资源耗尽等场景是否处理？ |
 | 4 | 风险遗漏 | 是否有未识别的风险（安全/性能/兼容性/数据丢失）？ |
 | 5 | 落地可行性 | 接口签名/数据结构/依赖关系是否可编译可运行？ |
-| 6 | 现有实现对照 | 是否复用了既有模式？是否与周围代码风格一致？ |
+| 6 | 现有实现对照 | 是否存在等价行为？命中处是否已追到调用链和实际行为？应复用、补充还是确需新增？ |
 | 7 | 跨文件一致性 | 接口变更是否全链路同步？上下游数据结构是否对齐？ |
 
 ### 第 3 层·自由探索（Free）
diff --git a/requesting-code-review/code-reviewer.md b/requesting-code-review/code-reviewer.md
index 08bf9c4..7fcfd70 100644
--- a/requesting-code-review/code-reviewer.md
+++ b/requesting-code-review/code-reviewer.md
@@ -5,7 +5,7 @@
 **用途：** 在工作成果扩散到更多工作之前，对照需求和代码质量标准做一次审查。
 
 ```
-Task tool（general-purpose）:
+当前平台的代码审查子代理（Codex 使用 `spawn_agent`；任务包自包含时使用 `fork_turns="none"`）：
   description: "审查代码改动"
   prompt: |
     你是一名资深代码审查员，精通软件架构、设计模式与最佳实践。
@@ -19,15 +19,13 @@ Task tool（general-purpose）:
 
     {PLAN_OR_REQUIREMENTS}
 
-    ## 待审查的 Git 范围
+    ## 待审查范围
 
-    **Base：** {BASE_SHA}
-    **Head：** {HEAD_SHA}
+    {REVIEW_SCOPES}
 
-    ```bash
-    git diff --stat {BASE_SHA}..{HEAD_SHA}
-    git diff {BASE_SHA}..{HEAD_SHA}
-    ```
+    每个 scope 必须给出 Git 根、BASE/HEAD、工作区状态，以及覆盖 committed、
+    staged、unstaged、untracked 改动的审查包或等价 diff。逐个 scope 审查；
+    父仓库干净不能替代嵌套 Git 根证据。缺少任一范围时标为待验证，不能静默略过。
 
     ## 检查内容
 
@@ -121,8 +119,7 @@ Task tool（general-purpose）:
 **占位符说明：**
 - `{DESCRIPTION}` —— 已构建内容的简要说明
 - `{PLAN_OR_REQUIREMENTS}` —— 预期功能（计划文件路径、任务文本或需求）
-- `{BASE_SHA}` —— 起始 commit
-- `{HEAD_SHA}` —— 结束 commit
+- `{REVIEW_SCOPES}` —— 每个 Git 根的路径、基线、状态和完整审查材料
 
 **审查员返回：** 优点、问题（Critical / Important / Minor）、建议、评估
 
diff --git a/subagent-driven-development/SKILL.md b/subagent-driven-development/SKILL.md
index c54d654..454e286 100644
--- a/subagent-driven-development/SKILL.md
+++ b/subagent-driven-development/SKILL.md
@@ -12,7 +12,7 @@ metadata:
 
 通过为每个任务分派一个全新的实现子智能体来执行计划：每个任务完成后做一次任务审查（规格合规性 + 代码质量），全部任务结束后再做一次覆盖整个分支的宽范围审查。
 
-**为什么用子智能体：** 你把任务委派给具有隔离上下文的专用智能体。通过精心设计它们的指令和上下文，确保它们专注并成功完成任务。它们绝不应继承你会话的上下文或历史记录——你要精确构造它们所需的一切。这样也能为你自己保留用于协调工作的上下文。
+**为什么用子智能体：** 你把任务委派给具有隔离上下文的专用智能体。通过精心设计它们的指令和上下文，确保它们专注并成功完成任务。任务简报完整时，子智能体不应继承会话历史；Codex 调用 `spawn_agent` 时使用 `fork_turns="none"`。如果当前接口无法控制继承范围，必须在提示词中明确只以任务简报和全局约束为准。
 
 **核心原则：** 每个任务一个全新子智能体 + 任务审查（规格 + 质量）+ 结尾宽范围审查 = 高质量、快速迭代
 
@@ -57,7 +57,7 @@ digraph process {
         "分派实现子智能体 (./implementer-prompt.md)" [shape=box];
         "实现子智能体有疑问?" [shape=diamond];
         "回答问题，提供上下文" [shape=box];
-        "实现子智能体实现、测试、提交、自审" [shape=box];
+        "实现子智能体实现、验证、按授权提交、自审" [shape=box];
         "写出 diff 文件，分派任务审查子智能体 (./task-reviewer-prompt.md)" [shape=box];
         "任务审查者报告规格 ✅ 且质量通过?" [shape=diamond];
         "针对 关键/重要 问题分派修复子智能体" [shape=box];
@@ -73,8 +73,8 @@ digraph process {
     "分派实现子智能体 (./implementer-prompt.md)" -> "实现子智能体有疑问?";
     "实现子智能体有疑问?" -> "回答问题，提供上下文" [label="是"];
     "回答问题，提供上下文" -> "分派实现子智能体 (./implementer-prompt.md)";
-    "实现子智能体有疑问?" -> "实现子智能体实现、测试、提交、自审" [label="否"];
-    "实现子智能体实现、测试、提交、自审" -> "写出 diff 文件，分派任务审查子智能体 (./task-reviewer-prompt.md)";
+    "实现子智能体有疑问?" -> "实现子智能体实现、验证、按授权提交、自审" [label="否"];
+    "实现子智能体实现、验证、按授权提交、自审" -> "写出 diff 文件，分派任务审查子智能体 (./task-reviewer-prompt.md)";
     "写出 diff 文件，分派任务审查子智能体 (./task-reviewer-prompt.md)" -> "任务审查者报告规格 ✅ 且质量通过?";
     "任务审查者报告规格 ✅ 且质量通过?" -> "针对 关键/重要 问题分派修复子智能体" [label="否"];
     "针对 关键/重要 问题分派修复子智能体" -> "写出 diff 文件，分派任务审查子智能体 (./task-reviewer-prompt.md)" [label="重新审查"];
@@ -120,7 +120,7 @@ digraph process {
 
 实现子智能体会报告四种状态之一。对每种状态做相应处理：
 
-**DONE：** 生成审查包（在本技能目录下运行 `scripts/review-package BASE HEAD`——它会打印出自己写入的那个唯一文件路径；BASE 是你在分派实现者之前记录下来的那个提交——**绝不用** `HEAD~1`，那会悄悄丢掉多提交任务里除最后一个之外的所有提交），然后把打印出的路径交给任务审查者去分派。
+**DONE：** 生成审查包（在本技能目录下运行 `scripts/review-package BASE HEAD`——它会同时包含提交区间、当前 staged/unstaged diff 和未跟踪文件；BASE 是分派实现者前记录的提交，**绝不用** `HEAD~1`），然后把打印出的唯一路径交给任务审查者。未授权 commit 时，审查包是从 BASE 到当前工作区的累计视图；审查者必须结合本任务简报和实现者报告聚焦本任务文件，不能因为 HEAD 未变化而判定“无改动”。
 
 **DONE_WITH_CONCERNS：** 实现者完成了工作但标记了疑虑。在继续之前先读这些疑虑。如果疑虑涉及正确性或范围，在审查前先解决。如果只是观察性说明（例如"这个文件越来越大了"），记录下来并继续进入审查。
 
@@ -146,7 +146,7 @@ digraph process {
 - 不要让审查者去重跑实现者已经在同一份代码上跑过的测试——实现者的报告已经带着测试证据
 - 不要替审查者预判发现——绝不指示审查者去忽略或不上报某个具体问题。如果你认为某个发现会是误报，那就让审查者提出来，在审查循环里裁定它。如果你正在写的提示词里出现了"不要标记""别把 X 当缺陷""顶多算 Minor""计划选择了"——停下：你在预判，通常是为了省掉一轮审查。
 - 你交给审查者的全局约束块是它的注意力透镜。从计划的"全局约束"一节或规格里**逐字**抄下有约束力的需求：精确的取值、精确的格式、以及组件之间被明确规定的关系（"与 X 相同的布局""匹配 Y"）。审查者的模板里已经带着流程规则（YAGNI、测试卫生、审查方法）——约束块是留给**本项目**规格所要求的东西的。
-- 把 diff 作为文件交给审查者：运行本技能的 `scripts/review-package BASE HEAD`，把它打印出的文件路径交给审查者（若没有 bash：对该区间跑 `git log --oneline`、`git diff --stat`、`git diff -U10`，重定向到一个唯一命名的文件）。这些输出永远不会进入你自己的上下文，而审查者在一次 Read 调用里就能看到提交列表、stat 摘要和带上下文的完整 diff。用你在分派实现者之前记录下的 BASE——**绝不用** `HEAD~1`，那会悄悄截断多提交任务。
+- 把 diff 作为文件交给审查者：运行本技能的 `scripts/review-package BASE HEAD`。包内必须同时包含 BASE..HEAD 提交、从 BASE 到当前工作区的 tracked diff、状态和未跟踪文件；若没有 bash，手工生成等价内容。未授权 commit 时这是累计工作区视图，审查者按任务简报和报告聚焦本任务；不得退回只审 `BASE..HEAD` 的旧做法。
 - 一份分派提示词描述的是**一个任务**，不是会话的历史。不要把累积的前序任务小结（"任务 1-3 之后的状态"）粘进后续分派里——真实会话里有一次分派冲到了 42k 字符，其中 99% 是粘进去的历史。一个全新的子智能体需要的是：它的任务、它要接触的接口、以及全局约束。别的都不要。
 - 针对 关键 和 重要 的发现分派修复子智能体。把 次要 的发现随手记进进度账本，并让最终的整分支审查指向那份清单，让它去分诊哪些必须在合并前修掉。没人读的汇总等于悄悄丢弃。
 - 一个被标为"计划强制"的发现——或任何与计划文本要求相冲突的发现——是人类的决定，就像任何计划矛盾一样：把发现和计划原文一起呈上，问哪一方说了算。不要因为计划强制了它就驳回这个发现，也不要在不问的情况下分派一个与计划相冲突的修复。
@@ -169,9 +169,8 @@ digraph process {
 
 - 技能启动时，检查是否有账本：
   `cat "$(git rev-parse --show-toplevel)/.superpowers/sdd/progress.md"`。在那里被列为完成的任务就是完成了——不要重新分派它们；从第一个未标记完成的任务处继续。
-- 当某个任务的审查干净地返回时，在你做其他记账的同一条消息里，往账本追加一行：
-  `Task N: complete (commits <base7>..<head7>, review clean)`。
-- 这个账本是你的恢复地图：它点名的那些提交，即使你的上下文已经不记得创建过它们，也确实存在于 git 中。压缩之后，相信账本和 `git log`，而不是你自己的记忆。
+- 当某个任务的审查干净地返回时，在账本追加一行：已授权并提交时记录 `commits <base7>..<head7>`；未提交时记录 `worktree at <head7>, review package <path>`，并列出本任务文件。
+- 这个账本是恢复地图。压缩后同时核对账本、`git log`、`git status` 和当前 diff；未授权 commit 的任务不能只靠提交历史恢复。
 - `git clean -fdx` 会毁掉这个账本（它是被 git 忽略的临时文件）；万一发生了，就从 `git log` 恢复。
 
 ## 提示词模板
@@ -185,6 +184,8 @@ digraph process {
 ```
 你：我正在使用子智能体驱动开发来执行这个计划。
 
+[本示例假设用户已授权按任务提交；未授权时由审查包携带累计工作区改动]
+
 [一次性读取计划文件：docs/superpowers/plans/feature-plan.md]
 [为所有任务创建待办]
 
@@ -246,7 +247,7 @@ digraph process {
 ## 优势
 
 **与手动执行相比：**
-- 子智能体自然遵循 TDD
+- 子智能体按任务简报执行用户已授权的验证策略
 - 每个任务全新上下文（不会混淆）
 - 并行安全（子智能体不会互相干扰）
 - 子智能体可以提问（工作前和工作中都可以）
@@ -316,7 +317,7 @@ digraph process {
 - **superpowers:finishing-a-development-branch** - 所有任务完成后收尾
 
 **子智能体应使用：**
-- **superpowers:test-driven-development** - 子智能体对每个任务遵循 TDD
+- **superpowers:test-driven-development** - 仅当用户明确要求编写单元测试、补充测试用例或完善覆盖时使用
 
 **替代工作流：**
 - **superpowers:executing-plans** - 用于并行会话而非同会话执行
diff --git a/subagent-driven-development/implementer-prompt.md b/subagent-driven-development/implementer-prompt.md
index ed600ad..28de349 100644
--- a/subagent-driven-development/implementer-prompt.md
+++ b/subagent-driven-development/implementer-prompt.md
@@ -3,7 +3,7 @@
 分派实现子智能体时使用此模板。
 
 ```
-Subagent (general-purpose):
+Subagent（使用当前平台的子代理工具；Codex 的任务简报自包含时设置 `fork_turns="none"`）：
   description: "实现任务 N：[任务名称]"
   model: [模型 —— 必填：按 SKILL.md 的"模型选择"来选；省略模型会默默
          继承会话里最贵的那个]
@@ -35,7 +35,7 @@ Subagent (general-purpose):
     1. 严格按照任务指定的内容实现
     2. 编写测试（如果任务要求则遵循 TDD）
     3. 验证实现是否正常工作
-    4. 提交你的工作
+    4. 只有任务简报明确写明用户已授权提交时，才提交你的工作
     5. 自审（见下文）
     6. 汇报
 
@@ -44,8 +44,8 @@ Subagent (general-purpose):
     **工作过程中：** 如果遇到意料之外或不清楚的情况，**提问**。
     随时可以暂停并澄清。不要猜测或做假设。
 
-    迭代过程中，只跑你正在改动的那部分的聚焦测试；在提交前跑一次
-    完整测试套件，而不是每次编辑后都跑。
+    迭代过程中优先运行覆盖当前改动的现有验证；任务要求完整测试套件时，
+    在交付前跑一次，而不是每次编辑后都跑。未明确要求时不得新增或修改测试文件。
 
     ## 代码组织
 
@@ -124,7 +124,7 @@ Subagent (general-purpose):
 
     然后只汇报以下内容（不超过 15 行——细节都在报告文件里）：
     - **状态：** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
-    - 创建的提交（短 SHA + 标题）
+    - 创建的提交（仅已授权并实际提交时填写；否则写“未授权提交，保留工作区改动”）
     - 一行测试小结（例如"14/14 通过，输出干净"）
     - 你的疑虑，如果有
     - 报告文件路径
diff --git a/subagent-driven-development/scripts/review-package b/subagent-driven-development/scripts/review-package
index 33bb20f..8498719 100755
--- a/subagent-driven-development/scripts/review-package
+++ b/subagent-driven-development/scripts/review-package
@@ -1,8 +1,8 @@
 #!/usr/bin/env bash
-# Generate a review package: commit list, stat summary, and the net
-# diff with extended context, written to a file the reviewer reads in one
-# call. Using the recorded per-task BASE (not HEAD~1) keeps multi-commit
-# tasks intact.
+# Generate a review package containing commits plus the current staged,
+# unstaged, and untracked worktree state. Using the recorded per-task BASE
+# (not HEAD~1) keeps multi-commit tasks intact; including the worktree keeps
+# uncommitted tasks reviewable when the user did not authorize commits.
 #
 # Usage: review-package BASE HEAD [OUTFILE]
 # Default OUTFILE: <repo-root>/.superpowers/sdd/review-<base7>..<head7>.diff
@@ -20,6 +20,13 @@ head=$2
 git rev-parse --verify --quiet "$base" >/dev/null || { echo "bad BASE: $base" >&2; exit 2; }
 git rev-parse --verify --quiet "$head" >/dev/null || { echo "bad HEAD: $head" >&2; exit 2; }
 
+resolved_head=$(git rev-parse "$head")
+current_head=$(git rev-parse HEAD)
+if [ "$resolved_head" != "$current_head" ]; then
+  echo "HEAD changed: requested $resolved_head, current $current_head" >&2
+  exit 2
+fi
+
 if [ $# -eq 3 ]; then
   out=$3
 else
@@ -28,16 +35,32 @@ else
 fi
 
 {
-  echo "# Review package: ${base}..${head}"
+  echo "# Review package: ${base}..${head} + current worktree"
   echo
   echo "## Commits"
   git log --oneline "${base}..${head}"
   echo
-  echo "## Files changed"
-  git diff --stat "${base}..${head}"
+  echo "## Worktree status"
+  git status --short
+  echo
+  echo "## Tracked files changed since BASE"
+  git diff --stat "$base"
+  echo
+  echo "## Tracked diff since BASE"
+  git diff --binary -U10 "$base"
   echo
-  echo "## Diff"
-  git diff -U10 "${base}..${head}"
+  echo "## Untracked files"
+  while IFS= read -r -d '' file; do
+    printf '\n### %s\n\n' "$file"
+    if [ -d "$file" ]; then
+      echo "Untracked directory or nested Git root; review it as a separate scope."
+      continue
+    fi
+    git diff --no-index --binary -- /dev/null "$file" || {
+      rc=$?
+      [ "$rc" -eq 1 ] || exit "$rc"
+    }
+  done < <(git ls-files --others --exclude-standard -z)
 } > "$out"
 
 commits=$(git rev-list --count "${base}..${head}")
diff --git a/subagent-driven-development/task-reviewer-prompt.md b/subagent-driven-development/task-reviewer-prompt.md
index 02d51c0..1f3bea1 100644
--- a/subagent-driven-development/task-reviewer-prompt.md
+++ b/subagent-driven-development/task-reviewer-prompt.md
@@ -6,7 +6,7 @@
 **目的：** 核实一个任务的实现与其需求匹配（不多不少）且构建良好（整洁、有测试、可维护）
 
 ```
-Subagent (general-purpose):
+Subagent（使用当前平台的子代理工具；Codex 的审查包自包含时设置 `fork_turns="none"`）：
   description: "审查任务 N（规格 + 质量）"
   model: [模型 —— 必填：按 SKILL.md 的"模型选择"来选；省略模型会默默
          继承会话里最贵的那个]
@@ -32,12 +32,14 @@ Subagent (general-purpose):
     **Head：** [HEAD_SHA]
     **Diff 文件：** [DIFF_FILE]
 
-    一次性读取这个 diff 文件——它包含提交列表、stat 摘要，以及
-    带上下文的完整 diff，它就是你对本次改动的视图。diff 的上下文行
+    一次性读取这个 diff 文件——它包含提交列表、工作区状态、从 BASE
+    到当前工作区的 tracked diff，以及未跟踪文件内容/独立 scope 提示。
+    它就是你对本次改动的视图。diff 的上下文行
     **就是**那些被改动的文件：不要单独去 Read 某个被改动的文件，除非
     你必须判断的某个 hunk 在函数中途被截断——并在报告中说明这一点。
-    不要重跑 git 命令。如果 diff 文件缺失，就自己取 diff：
-    `git diff --stat [BASE_SHA]..[HEAD_SHA]` 和 `git diff [BASE_SHA]..[HEAD_SHA]`。
+    不要重跑 git 命令。如果 diff 文件缺失，就自己取 `git status --short`、
+    `git diff --stat [BASE_SHA]`、`git diff [BASE_SHA]` 和未跟踪文件清单；
+    不能退回只看 `[BASE_SHA]..[HEAD_SHA]`。
     不要爬取更广的代码库。只有为了评估一个你能点名的具体风险，才去
     查看 diff 之外的代码——每个点名的风险做一次聚焦检查，并在报告中
     同时点名这个风险和你检查了什么。横切改动是正当的、可点名的风险：
@@ -57,7 +59,7 @@ Subagent (general-purpose):
 
     ## 测试
 
-    实现者已经跑过测试，并为正是这份代码报告了带 TDD 证据的结果。
+    实现者应按任务授权运行验证；只有任务明确要求 TDD 时，报告才应包含 TDD 证据。
     不要为了确认他们的报告而重跑测试套件。只有当阅读代码引出一个
     现有任何运行都无法回答的具体疑问时，才去跑测试——而且是聚焦
     测试，绝不是包级套件、竞态检测运行、或反复的/高次数的循环。
diff --git a/using-git-worktrees/SKILL.md b/using-git-worktrees/SKILL.md
index 4af7d35..86ded21 100644
--- a/using-git-worktrees/SKILL.md
+++ b/using-git-worktrees/SKILL.md
@@ -91,7 +91,7 @@ git rev-parse --show-superproject-working-tree 2>/dev/null
 git check-ignore -q .worktrees 2>/dev/null || git check-ignore -q worktrees 2>/dev/null
 ```
 
-**如果未被忽略：** 添加到 .gitignore，提交该改动，然后继续。
+**如果未被忽略：** 停止并向用户说明需要修改忽略规则。只有用户授权后才修改 `.gitignore` 或本地 exclude；commit 需要单独的明确授权。忽略校验通过前不得创建项目内 worktree。
 
 **为什么关键：** 防止 worktree 内容被意外提交到仓库。
 
@@ -105,7 +105,7 @@ git worktree add "$path" -b "$BRANCH_NAME"
 cd "$path"
 ```
 
-**沙盒回退：** 如果 `git worktree add` 因权限错误（沙盒拒绝）失败，告诉用户沙盒阻止了 worktree 创建，你将在当前目录原地工作。然后原地运行 setup 和基线测试。
+**创建失败：** 如果 `git worktree add` 因权限、路径或环境限制失败，停止并报告原始错误。只有用户明确同意改为当前目录实现后，才能原地运行 setup 和基线验证；不得自动扩大到原工作区。
 
 ## 步骤 2：项目设置
 
diff --git a/using-superpowers/SKILL.md b/using-superpowers/SKILL.md
index 245a01b..b493758 100644
--- a/using-superpowers/SKILL.md
+++ b/using-superpowers/SKILL.md
@@ -98,16 +98,17 @@ metadata:
 
 > 本段提炼自 icode-skill `SKILL.md`「工具调用模式规范」段，泛化去除 icode 专属的 MCP 调用覆盖强制化关联，保留"批量并行 + 连续执行"核心规则。平台中性，Claude Code / Codex CLI 均适用。
 
-## 深度思考载体分档
+## 重要决策记录
 
-重要判断/决策之前，先做结构化深度思考（不少于 3 步，每步对应当前任务的子项）。思考载体按运行环境分主备两档，**MCP 不可用时必须降级到文字块，不得跳过思考**：
+重要判断或方案取舍前，先完成结构化分析，但**不得要求输出或暴露模型的内部推理过程**。对用户和后续审查者只保留可复核的决策记录：
 
-- **主档（MCP 可用时）**：调用思考类 MCP 工具
-- **备档（MCP 不可用，如 Codex 第三方模型）**：输出结构化文字思考块（显式标注"思考："起头，分步骤列出）
+- 已确认事实及证据来源
+- 仍待验证的假设或限制
+- 采用的方案、主要取舍和验证方式
 
-凭记忆直接产出 = 思考降级。显式思考是深度推理的前置仪式。
+当前平台提供思考类工具时可按需使用；工具不可用不构成阻塞，也不要求用显式“思考块”代替。需要跟踪多步骤工作时使用当前平台的计划/待办工具。
 
-> 本段提炼自 icode-skill `references/thinking.md`，原文件含各步骤思考子项清单与历史参考分流标注。
+> 本段吸收 icode-skill 的结构化分析思想，但去除了强制 MCP 和显式推理留痕要求。
 
 ## 平台适配
 
diff --git a/using-superpowers/references/codex-tools.md b/using-superpowers/references/codex-tools.md
index 36c610b..d43b19c 100644
--- a/using-superpowers/references/codex-tools.md
+++ b/using-superpowers/references/codex-tools.md
@@ -7,19 +7,23 @@ Skills 使用 Claude Code 的工具名称。在 Codex 中遇到这些名称时
 | `Task` 工具（派遣子 agent） | `spawn_agent` |
 | 多个 `Task` 调用（并行） | 多个 `spawn_agent` 调用 |
 | Task 返回结果 | `wait_agent` |
-| Task 自动完成 | `close_agent` 释放槽位 |
+| Task 状态检查 | `list_agents`；等待超时不代表子 agent 已失败 |
 | `TodoWrite`（任务跟踪） | `update_plan` |
 | `Skill` 工具（调用 skill） | Skills 原生加载——直接按说明操作 |
 | `Read`、`Write`、`Edit`（文件） | 使用原生文件工具 |
 | `Bash`（执行命令） | 使用原生 shell 工具 |
 
-## 子 Agent 派遣需要多 Agent 支持
+## 子 Agent 派遣
 
-在 Codex 配置文件（`~/.codex/config.toml`）中添加：
+以**当前会话实际暴露且可调用的工具**为准。配置文件只能用于排查“为什么当前会话没有该工具”，不能单独证明子 agent 可用或不可用。
+
+当任务简报已包含完整上下文、且需要隔离上下文时，调用 `spawn_agent` 应使用 `fork_turns="none"`；如果子 agent 必须理解最近对话，才选择最小必要的正数或 `all`。
+
+部分 Codex 版本可通过以下配置启用多 agent：
 
 ```toml
 [features]
 multi_agent = true
 ```
 
-启用后可使用 `spawn_agent`、`wait_agent` 和 `close_agent`，支持 `dispatching-parallel-agents` 和 `subagent-driven-development` 等 skills。使用 subagent-driven-development 时，implementer 和 reviewer 子 agent 完成全部工作后应始终 `close_agent` 释放。
+配置生效后仍需重新启动会话，并检查当前工具面是否实际包含 `spawn_agent`、`wait_agent`、`list_agents` 等接口。只有当前接口确实提供清理/关闭工具时才调用；未提供时不要虚构 `close_agent`。
diff --git a/verification-before-completion/SKILL.md b/verification-before-completion/SKILL.md
index 9dcdf7b..50caa16 100644
--- a/verification-before-completion/SKILL.md
+++ b/verification-before-completion/SKILL.md
@@ -42,6 +42,19 @@ metadata:
 跳过任何一步 = 说谎，不是验证
 ```
 
+## 证据新鲜度与增量修改
+
+验证证据只对它实际覆盖的那版产物有效。完成验证后又发生任何代码、配置、计划或依赖变化时：
+
+1. 旧证据对受影响结论自动失效，不得继续引用为“已通过”。
+2. 重新读取受影响文件和当前 diff，判断哪些调用链、需求点和验证命令需要重跑。
+3. 至少重跑覆盖新增改动及其上下游影响的验证；无法重跑的部分标“待验证”并说明原因。
+4. 更新交付摘要，明确旧结论是否仍成立；不得只追加“补了一点修改”而沿用旧完成声明。
+
+## 多 Git 根验证边界
+
+对所有改动文件所在目录执行 `git -C <改动文件所在目录> rev-parse --show-toplevel`，按实际 Git 根分别检查 status、diff 和验证结果。父仓库干净不证明嵌套仓库、submodule 或独立模块干净；每个受影响 Git 根都必须单独给出证据或待验证说明。
+
 ## 常见失败模式
 
 | 结论 | 需要 | 不够格 |
@@ -86,12 +99,14 @@ metadata:
 ❌ "应该能通过了" / "看起来对了"
 ```
 
-**回归测试（TDD 红-绿）：**
+**回归测试（仅用户明确要求新增/修改测试时）：**
 ```
 ✅ 编写 → 运行（通过）→ 回退修复 → 运行（必须失败）→ 恢复 → 运行（通过）
 ❌ "我写了回归测试"（没有经过红-绿验证）
 ```
 
+用户未明确要求新增或修改测试时，不创建回归测试；改用现有测试、构建、静态检查或原问题复现路径验证，并如实说明验证边界。
+
 **构建：**
 ```
 ✅ [运行构建] [看到：exit 0] "构建通过"
@@ -154,35 +169,36 @@ metadata:
 
 > 本段提炼自 icode-skill `references/adversarial.md`「证据回指纪律」段，原规则：每条 confirmed 结论必须能回指到具体证据片段（计划章节/代码 file:line/日志节点+时间+原文）。
 
-## 需求收敛双视角
+## 需求收敛三视角
 
-宣称"需求已满足"之前，必须从两个独立视角逐条对照，防止"计划功能点都实现了，但用户原始需求的某个边角被遗漏"：
+宣称"需求已满足"之前，必须从三个独立视角逐条对照，既防需求遗漏，也防重复实现：
 
 | 视角 | 对照基线 | 检查问题 |
 |------|---------|---------|
 | **A·需求角度** | 用户原始需求（对话/规格/issue 原文） | 用户想要的每一条，最终产物是否满足？ |
 | **B·计划角度** | 实现计划/任务清单 | 计划定义的每个功能点，代码是否落地？ |
+| **C·必要性角度** | 最终产物与工程现有行为 | 新增功能是否已有等价实现？应复用、补充还是确需新增？ |
 
 **操作步骤：**
 
 1. **拆解需求**：重读用户原始需求，逐条拆解为可验证的需求点
-2. **双视角对照**：每个需求点同时在两个视角下给出判定
+2. **三视角对照**：每个需求点同时从需求、计划和现有实现三个视角给出判定；视角 C 的文本命中必须追到调用链和实际行为
 
 ```markdown
-| 需求点 | 视角 B（计划角度） | 视角 A（需求角度） | 收敛判定 |
-|--------|------------------|------------------|---------|
-| <需求 1> | 计划 §X.Y -> 代码 file:line ✅ | 用户原话 -> 产物满足 ✅ | ✅ 收敛 |
-| <需求 2> | 计划 §X.Z -> 代码 file:line ✅ | 用户原话 -> 边缘 case 未覆盖 ❌ | ❌ 追加修复 |
-| <需求 3> | - | 用户原话 -> 计划遗漏 ❌ | ❌ 追加修复 |
+| 需求点 | A·需求角度 | B·计划角度 | C·必要性角度 | 收敛判定 |
+|--------|------------|------------|----------------|---------|
+| <需求 1> | 用户原话 -> 产物满足 ✅ | 计划 §X.Y -> 代码 file:line ✅ | 无等价行为 ✅ | ✅ 收敛 |
+| <需求 2> | 边缘 case 未覆盖 ❌ | 计划 §X.Z -> 代码 file:line ✅ | 部分覆盖，边界已说明 | ❌ 追加修复 |
+| <需求 3> | 用户需求已存在 | 计划遗漏 ❌ | 现有入口已完整覆盖 | ❌ 改为复用/取消新增 |
 ```
 
-3. **收敛判定**：两视角都通过才算收敛；任一未通过 -> 追加为修复任务，不得跳过
+3. **收敛判定**：三个视角都通过才算收敛；任一未通过 -> 修正计划、改为复用或追加修复任务，不得跳过
 
-**两视角的区别**：视角 B 检查"计划定义的做了没"，视角 A 检查"用户想要的做了没"。两者不替代--计划可能遗漏用户需求的某个维度，计划功能点全部实现不代表用户需求全部满足。
+**三个视角的区别**：B 检查“计划定义的做了没”，A 检查“用户想要的做了没”，C 检查“做的事情是否本来就已有实现”。三者互不替代。
 
-**与门控函数的关系**：门控函数要求"确定什么命令能证明结论"，双视角要求"确定什么需求点需要验证"。双视角是门控函数在需求维度的细化--把笼统的"需求已满足"拆成逐条可验证的对照表。
+**与门控函数的关系**：门控函数要求“什么证据能证明结论”，三视角要求“哪些需求点、计划点和必要性需要验证”，把笼统的“需求已满足”拆成可复核的对照表。
 
-> 本段提炼自 icode-skill `steps/06_audit.md` §6.7「原始需求收敛」双视角对照机制，原规则在 icode 终审步骤执行，依赖 metadata.requirement / limit_refs 字段。此处泛化去除 icode 专属概念，保留双视角对照核心规则。
+> A/B 视角提炼自 icode-skill 原始需求收敛机制；C 视角提炼自 v2.11 `references/necessity_check.md`，均已去除 metadata 和工单状态依赖。
 
 ## 底线
 
diff --git a/writing-plans/SKILL.md b/writing-plans/SKILL.md
index d3c1bff..956c207 100644
--- a/writing-plans/SKILL.md
+++ b/writing-plans/SKILL.md
@@ -12,16 +12,15 @@ metadata:
 
 ## 概述
 
-编写全面的实现计划，假设工程师对我们的代码库零上下文，且品味存疑。记录他们需要知道的一切：每个任务要修改哪些文件、代码、测试、可能需要查阅的文档、如何测试。将整个计划拆成小步骤任务。DRY。YAGNI。TDD。频繁 commit。
+编写全面的实现计划，假设工程师对我们的代码库零上下文，且品味存疑。记录他们需要知道的一切：每个任务要修改哪些文件、代码、可能需要查阅的文档，以及如何验证。将整个计划拆成小步骤任务。DRY。YAGNI。
 
 假设他们是有经验的开发者，但对我们的工具链和问题领域几乎一无所知。假设他们不太擅长测试设计。
 
 **开始时宣布：** "我正在使用 writing-plans 技能创建实现计划。"
 
-**上下文：** 此技能应在专用 worktree 中运行（由 brainstorming 技能创建）。
+**上下文：** 编写计划本身不要求创建 worktree。进入实现前如需隔离，由 `using-git-worktrees` 按用户授权和项目规则处理。
 
-**计划保存位置：** `docs/superpowers/plans/YYYY-MM-DD-<feature-name>.md`
-- （用户对计划位置的偏好优先于此默认值）
+**计划落盘：** 只有用户要求、项目惯例或已批准流程需要时才保存；默认位置为 `docs/superpowers/plans/YYYY-MM-DD-<feature-name>.md`，用户偏好优先。
 
 ## 范围检查
 
@@ -40,12 +39,12 @@ metadata:
 
 ## 小步骤任务粒度
 
-**每步是一个操作（2-5 分钟）：**
-- "编写失败的测试" - 一步
-- "运行它确认失败" - 一步
-- "实现最少代码让测试通过" - 一步
-- "运行测试确认通过" - 一步
-- "Commit" - 一步
+**每步是一个可独立验证的操作（通常 2-5 分钟）：**
+- 阅读目标符号及调用链 - 一步
+- 完成一个最小实现改动 - 一步
+- 运行与该改动匹配的现有验证命令 - 一步
+- 只有用户明确要求测试时，才加入新增/修改测试及 TDD 步骤
+- 只有用户明确授权提交时，才加入 Commit 步骤
 
 ## 计划文档头部
 
@@ -71,39 +70,31 @@ metadata:
 ### 任务 N：[组件名称]
 
 **文件：**
-- 创建：`exact/path/to/file.py`
 - 修改：`exact/path/to/existing.py:123-145`
-- 测试：`tests/exact/path/to/test.py`
+- 关联：`exact/path/to/caller.py:42`
+- 测试：`tests/exact/path/to/test.py`（仅用户明确要求新增/修改测试时列出）
 
-- [ ] **步骤 1：编写失败的测试**
+- [ ] **步骤 1：核对现有实现和调用链**
 
-```python
-def test_specific_behavior():
-    result = function(input)
-    assert result == expected
+```bash
+rg -n "function_name|related_symbol" exact/path
 ```
 
-- [ ] **步骤 2：运行测试验证失败**
-
-运行：`pytest tests/path/test.py::test_name -v`
-预期：FAIL，报错 "function not defined"
-
-- [ ] **步骤 3：编写最少实现代码**
+- [ ] **步骤 2：实施最小修改**
 
-```python
-def function(input):
-    return expected
+```text
+在这里给出本任务需要落地的精确代码或逐行修改说明；不得使用 TODO 或省略号占位。
 ```
 
-- [ ] **步骤 4：运行测试验证通过**
+- [ ] **步骤 3：运行现有验证**
 
-运行：`pytest tests/path/test.py::test_name -v`
-预期：PASS
+运行：`<项目已有的构建、静态检查、测试或复现命令>`
+预期：`<明确的退出码或输出判据>`
 
-- [ ] **步骤 5：Commit**
+- [ ] **步骤 4：Commit（仅用户已授权时保留）**
 
 ```bash
-git add tests/path/test.py src/path/file.py
+git add exact/path/to/existing.py exact/path/to/caller.py
 git commit -m "feat: add specific feature"
 ```
 ````
@@ -122,7 +113,8 @@ git commit -m "feat: add specific feature"
 - 始终使用精确的文件路径
 - 每个步骤都包含完整代码——如果步骤涉及代码变更，就展示代码
 - 精确的命令和预期输出
-- DRY、YAGNI、TDD、频繁 commit
+- DRY、YAGNI
+- 测试和 commit 都服从用户授权边界：未明确要求时不新增/修改测试，不把 commit 写成强制步骤
 
 ## 自检
 
@@ -138,9 +130,9 @@ git commit -m "feat: add specific feature"
 
 ## 执行交接
 
-保存计划后，提供执行选项：
+计划完成后提供执行选项。已落盘时给出路径；未要求落盘时直接以当前对话中的计划为准：
 
-**"计划已完成并保存到 `docs/superpowers/plans/<filename>.md`。两种执行方式：**
+**"计划已完成。两种执行方式：**
 
 **1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行审查，快速迭代
 
diff --git a/writing-plans/plan-document-reviewer-prompt.md b/writing-plans/plan-document-reviewer-prompt.md
index af97df3..4b441d5 100644
--- a/writing-plans/plan-document-reviewer-prompt.md
+++ b/writing-plans/plan-document-reviewer-prompt.md
@@ -7,7 +7,7 @@
 **调度时机：** 完整计划编写完成后。
 
 ```
-Task tool（通用）:
+当前平台的审查子代理（Codex 使用 `spawn_agent`；审查材料自包含时设置 `fork_turns="none"`）：
   description: "审查计划文档"
   prompt: |
     你是一名计划文档审查员。验证此计划是否完整并准备好进行实现。
diff --git a/writing-skills/SKILL.md b/writing-skills/SKILL.md
index 04ca10c..3de1142 100644
--- a/writing-skills/SKILL.md
+++ b/writing-skills/SKILL.md
@@ -20,7 +20,7 @@ metadata:
 
 **核心原则：** 如果你没有观察到智能体在没有该技能时失败，你就不知道这个技能是否教了正确的东西。
 
-**必需背景：** 在使用此技能前，你必须理解 superpowers:test-driven-development。该技能定义了基本的红-绿-重构循环。本技能将 TDD 适配到文档编写中。
+本技能借用“红—绿—重构”作为流程文档的验证模型，但不因此自动触发代码 TDD，也不授权新增/修改项目测试文件。Skill 行为验证优先使用临时提示场景、现有静态检查或临时目录；用户对测试和文件修改的限制始终优先。
 
 **官方指南：** Anthropic 官方的技能编写最佳实践请参见 anthropic-best-practices.md。该文档提供了补充本技能 TDD 导向方法的额外模式和指南。
 
@@ -599,7 +599,7 @@ helper1、helper2、step3、pattern4
 
 ## 技能创建清单（TDD 适配版）
 
-**重要：使用 TodoWrite 为下面的每个清单项创建待办。**
+**重要：使用当前平台的计划/待办工具跟踪下面的适用项。**
 
 **红色阶段 - 编写失败的测试：**
 - [ ] 创建压力场景（纪律类技能需 3 个以上组合压力）
@@ -632,9 +632,9 @@ helper1、helper2、step3、pattern4
 - [ ] 无叙事性故事
 - [ ] 支持文件仅用于工具或大量参考
 
-**部署：**
-- [ ] 将技能提交到 git 并推送到你的 fork（如果已配置）
-- [ ] 考虑通过 PR 贡献回去（如果具有广泛用途）
+**部署（仅在用户明确授权相应操作时）：**
+- [ ] 用户授权 commit 时，将技能提交到 Git
+- [ ] 用户授权 push/PR 时，再推送到 fork 或创建 PR
 
 ## 发现工作流
 
