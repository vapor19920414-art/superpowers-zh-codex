diff --git a/brainstorming/SKILL.md b/brainstorming/SKILL.md
index 7ea7031..5c8408a 100644
--- a/brainstorming/SKILL.md
+++ b/brainstorming/SKILL.md
@@ -31,7 +31,7 @@ metadata:
 3. **提出澄清问题** — 每次一个，了解目的/约束/成功标准
 4. **提出 2-3 种方案** — 附带权衡分析和你的推荐
 5. **展示设计** — 按复杂度分节展示，每节展示后获得用户批准
-6. **编写设计文档** — 保存到 `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md` 并 commit
+6. **按需编写设计文档** — 用户要求、项目惯例或已批准流程需要落盘时，保存到约定位置；只有用户明确授权时才 commit
 7. **规格自检** — 快速内联检查占位符、矛盾、模糊性、范围（详见下方）
 8. **用户审查书面规格** — 在继续之前请用户审查规格文件
 9. **过渡到实现** — 调用 writing-plans 技能创建实现计划
@@ -82,12 +82,55 @@ digraph brainstorming {
 - 每条消息只提一个问题——如果一个主题需要更多探索，拆分成多个问题
 - 重点理解：目的、约束、成功标准
 
+**前提假设验证（防"外部前提当事实用"）：**
+
+在理解需求、提出方案之前，识别需求依赖的**外部事实**（无法纯靠代码验证的前提），防止"需求前提与事实不符但到实现/测试阶段才暴露"。
+
+**识别前提假设：** 逐条检查需求是否依赖以下类型的外部事实：
+- 硬件行为（传感器精度、外设时序、中断响应）
+- 数据格式（第三方协议、文件编码、字节序）
+- 第三方库行为（API 语义、版本兼容、边界条件）
+- 性能假设（吞吐量、延迟、资源占用）
+- 因果假设（"改了 X 就会 Y"的因果链）
+
+**验证分流：**
+
+| 前提类型 | 能用 Read/Grep 代码验证？ | 处理方式 |
+|---------|------------------------|---------|
+| 代码可验证（接口签名、宏定义、调用链） | 能 | 标注验证方法，直接实证，不建实验 |
+| 外部事实（硬件/数据格式/第三方行为/性能/因果） | 不能 | 必须设计最小实验验证 |
+
+**实验设计要求（对外部事实前提）：**
+1. **单一变量**：每次实验只改一个变量，避免归因不清
+2. **成功 + 失败信号**：每条假设同时定义"什么结果证明成立"和"什么结果证伪"
+3. **证伪回写**：假设被实验证伪时，必须回写修正需求（删除或重定义该前提），不得沿袭已证伪的假设进入方案设计
+4. **无外部前提时**：显式标注"无外部前提（均可代码验证）"，不静默跳过
+
+> 本段提炼自 icode-skill `references/anti_laziness.md` 第 27 条 + `steps/00_init.md` §5「前提假设实验表」，泛化去除 icode 专属的 `00_init.md §5` / `plan §9` 字段依赖，保留"识别外部前提 -> 代码能验证则标注验证方法、不能则设计实验 -> 证伪回写"核心规则。
+
+**现有功能覆盖度检查（防重复实现）：**
+
+对每个拟新增的功能、接口或模块，在方案设计前检查工程中是否已有等价行为：
+
+1. 用需求的“动词 + 名词”及同义词检索当前仓库及需求涉及的嵌套 Git 根，不只查当前模块；遵循项目忽略规则，不扫描生成物、依赖缓存或无关目录。
+2. 命中后读取完整函数/配置上下文，并追踪关键调用方和消费后的实际行为；文本命中不等于行为已覆盖。
+3. 给出带证据位置的三态结论：
+
+| 判定 | 处理方式 |
+|------|---------|
+| 已覆盖 | 复用现有实现或取消新增，不进入重复设计 |
+| 部分覆盖 | 明确现有边界和本次只需补齐的缺口 |
+| 未覆盖 | 记录检索范围和证据后，正常进入方案设计 |
+
+无法确认覆盖范围时标“待验证”，不得默认按未覆盖继续设计。
+
+> 本段提炼自 icode-skill `references/necessity_check.md`，去除了工单、metadata 和固定行数要求，保留“检索命中 → 行为链核实 → 三态判定”的核心机制。
+
 **探索方案：**
 
 - 提出 2-3 种不同的方案及其权衡
 - 以对话的方式展示选项，附上你的推荐和理由
 - 先展示你推荐的方案并解释原因
-- 严格遵循 YAGNI —— 从每个方案和设计里移除不必要的功能
 
 **展示设计：**
 
@@ -114,10 +157,10 @@ digraph brainstorming {
 
 **文档：**
 
-- 将验证通过的设计（规格说明）写入 `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`
-  - （用户对规格位置的偏好优先于此默认值）
+- 用户要求、项目惯例或已批准流程需要落盘时，将验证通过的设计写入 `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`
+  - 用户对是否落盘及规格位置的偏好优先于默认值
 - 如果可用，使用 elements-of-style:writing-clearly-and-concisely 技能
-- 将设计文档 commit 到 git
+- 只有用户明确要求或已批准计划明确授权时，才将设计文档 commit 到 Git
 
 **规格自检：**
 编写规格文档后，以全新的视角审视它：
@@ -132,7 +175,7 @@ digraph brainstorming {
 **用户审查关卡：**
 规格自检完成后，请用户在继续之前审查书面规格：
 
-> "规格已编写并 commit 到 `<path>`。请审查一下，如果在我们开始编写实现计划之前你想做任何修改，请告诉我。"
+> "规格已整理到 `<path>`（若未要求落盘，则直接在对话中给出）。请审查一下，如果在我们开始编写实现计划之前你想做任何修改，请告诉我。"
 
 等待用户回复。如果他们要求修改，做出修改并重新运行规格自检。只有在用户批准后才继续。
 
@@ -141,6 +184,15 @@ digraph brainstorming {
 - 调用 writing-plans 技能创建详细的实现计划
 - 不要调用任何其他技能。writing-plans 是下一步。
 
+## 核心原则
+
+- **每次一个问题** — 不要同时抛出多个问题
+- **优先选择题** — 在可能的情况下比开放式问题更容易回答
+- **严格遵循 YAGNI** — 从所有设计中移除不必要的功能
+- **探索替代方案** — 在做决定之前始终提出 2-3 种方案
+- **增量验证** — 展示设计，获得批准后再继续
+- **保持灵活** — 有不明确的地方就回头澄清
+
 ## 视觉伴侣
 
 一个基于浏览器的伴侣工具，用于在头脑风暴过程中展示原型、图表和视觉选项。它是一个工具——不是一种模式。接受伴侣意味着它可用于适合视觉呈现的问题；并不意味着每个问题都要通过浏览器。
diff --git a/brainstorming/spec-document-reviewer-prompt.md b/brainstorming/spec-document-reviewer-prompt.md
index 7e61864..ccecdf7 100644
--- a/brainstorming/spec-document-reviewer-prompt.md
+++ b/brainstorming/spec-document-reviewer-prompt.md
@@ -7,7 +7,7 @@
 **调度时机：** 规格文档写入 docs/superpowers/specs/ 之后
 
 ```
-Task tool（通用）:
+当前平台的审查子代理（Codex 使用 `spawn_agent`；审查材料自包含时设置 `fork_turns="none"`）：
   description: "审查规格文档"
   prompt: |
     你是一名规格文档审查员。验证此规格是否完整并准备好进行计划编写。
diff --git a/executing-plans/SKILL.md b/executing-plans/SKILL.md
index ea1a007..9c8d513 100644
--- a/executing-plans/SKILL.md
+++ b/executing-plans/SKILL.md
@@ -16,17 +16,16 @@ metadata:
 
 **开始时宣布：** "我正在使用 executing-plans 技能来实现此计划。"
 
-**注意：** 告诉你的人类伙伴，Superpowers 在有子代理支持时效果好得多（Claude Code、Codex CLI、Codex App、Copilot CLI 与 Gemini CLI 都算；见 `../using-superpowers/references/` 下的各平台工具参考）。如果子代理可用，请使用 superpowers:subagent-driven-development 而非此技能。
+**注意：** 告诉你的人类伙伴，Superpowers 在有子代理支持时效果好得多。如果在支持子代理的平台上运行（如 Claude Code 或 Codex），其工作质量会显著提高。如果子代理可用，请使用 superpowers:subagent-driven-development 而非此技能。
 
 ## 流程
 
 ### 步骤 1：加载并审查计划
 
-1. 确保有一个隔离的工作区：用 superpowers:using-git-worktrees 创建一个，或者核实已有的那个
-2. 读取计划文件
-3. 批判性审查——识别计划中的任何问题或疑虑
-4. 如果有疑虑：在开始之前向你的人类伙伴提出
-5. 如果没有疑虑：创建 TodoWrite 并继续
+1. 读取计划文件
+2. 批判性审查——识别计划中的任何问题或疑虑
+3. 如果有疑虑：在开始之前向你的人类伙伴提出
+4. 如果没有疑虑：创建 TodoWrite 并继续
 
 **审查时重点检查：**
 - 步骤之间是否有依赖遗漏？（A 依赖 B，但 B 排在 A 之后）
@@ -56,7 +55,7 @@ metadata:
 2. **理解目标** — 重读任务描述，明确完成标准
 3. **执行实现** — 严格按照计划步骤执行（计划已有小步骤）
 4. **运行验证** — 按要求运行测试或检查
-5. **提交变更** — 每完成一个任务提交一次，commit message 引用任务编号
+5. **按授权提交变更** — 只有用户明确要求或计划已明确授权时才提交；否则保留工作区改动供用户审查
 6. **标记为已完成** — 更新 TodoWrite
 
 **每个任务的节奏：**
@@ -78,7 +77,7 @@ $ npm test -- --grep "validation"
   ✓ 拒绝过长用户名 (5ms)
   3 passing
 
-[提交]
+[提交（本计划已获用户授权时）]
 $ git add src/middleware/validate.js tests/validation.test.js
 $ git commit -m "feat: 添加用户输入验证（任务 2/5）"
 
@@ -90,7 +89,29 @@ $ git commit -m "feat: 添加用户输入验证（任务 2/5）"
 - 执行过程中持续留意：整体方向还对吗？有没有偏离计划？
 - 如果发现前面的实现有问题，先修复再继续，不要带着问题往下走
 
-### 步骤 3：完成开发
+### 步骤 3：处理常见异常
+
+**测试失败：**
+1. 读错误信息，定位失败原因
+2. 区分：是实现 bug？还是测试本身有问题？还是计划描述有误？
+3. 实现 bug → 修复并重跑
+4. 测试有问题 → 先报告测试问题；只有用户明确要求编写/修改测试时才修复测试
+5. 计划有误 → 停下来，向伙伴报告并建议修正
+
+**依赖缺失：**
+```
+任务 3 需要 Redis 连接，但计划中没有提及 Redis 配置。
+→ 停止执行
+→ 向伙伴报告："任务 3 需要 Redis，计划中未包含配置步骤。
+   建议：在任务 3 前插入 '配置 Redis 连接' 步骤。"
+```
+
+**指令不清：**
+- 不要猜测意图，不要"合理推断"
+- 列出你的理解和困惑，让伙伴澄清
+- 等待回复后再继续
+
+### 步骤 4：完成开发
 
 所有任务完成并验证后：
 - 宣布："我正在使用 finishing-a-development-branch 技能来完成此工作。"
@@ -134,35 +155,6 @@ $ git commit -m "feat: 添加用户输入验证（任务 2/5）"
 
 **不确定时就问，不要猜测。**
 
-## 常见异常处理
-
-> 🇨🇳 **本节是 superpowers-zh 的增量内容，上游 obra/superpowers 没有。**
-> 它展开的是上一节「遇到阻塞（缺少依赖、测试失败、指令不清）」的三种具体情形。
-> 上游的步骤 1–3 与其余各节均为逐节翻译，未被本节改动。
-
-
-**测试失败：**
-1. 读错误信息，定位失败原因
-2. 区分：是实现 bug？还是测试本身有问题？还是计划描述有误？
-3. 实现 bug → 修复并重跑
-4. 测试有问题 → 修复测试，向伙伴说明
-5. 计划有误 → 停下来，向伙伴报告并建议修正
-
-**依赖缺失：**
-```
-任务 3 需要 Redis 连接，但计划中没有提及 Redis 配置。
-→ 停止执行
-→ 向伙伴报告："任务 3 需要 Redis，计划中未包含配置步骤。
-   建议：在任务 3 前插入 '配置 Redis 连接' 步骤。"
-```
-
-**指令不清：**
-- 不要猜测意图，不要"合理推断"
-- 列出你的理解和困惑，让伙伴澄清
-- 等待回复后再继续
-
-**提交粒度：** 每个任务单独提交，commit message 引用任务编号。
-
 ## 何时回到之前的步骤
 
 **回到审查（步骤 1）当：**
@@ -175,7 +167,14 @@ $ git commit -m "feat: 添加用户输入验证（任务 2/5）"
 - 先批判性审查计划
 - 严格按照计划步骤执行
 - 不要跳过验证
+- 只有用户明确授权时才提交；授权逐任务提交时，commit message 引用任务编号
 - 计划要求时引用相应技能
 - 遇到阻塞时停下来，不要猜测
 - 未经用户明确同意，绝不在 main/master 分支上开始实现
 
+## 集成
+
+**必需的工作流技能：**
+- **superpowers:using-git-worktrees** - 必需：开始前建立隔离的工作空间
+- **superpowers:writing-plans** - 创建此技能要执行的计划
+- **superpowers:finishing-a-development-branch** - 所有任务完成后收尾开发
diff --git a/receiving-code-review/SKILL.md b/receiving-code-review/SKILL.md
index 129a4a7..fed0167 100644
--- a/receiving-code-review/SKILL.md
+++ b/receiving-code-review/SKILL.md
@@ -209,3 +209,23 @@ metadata:
 
 在 GitHub 上回复行内审查评论时，在评论线程中回复（`gh api repos/{owner}/{repo}/pulls/{pr}/comments/{id}/replies`），不要发顶层 PR 评论。
 
+
+## 对反馈做对抗验证
+
+收到反馈后，对**判断性反馈**（非明确事实指正）做对抗评估，防确认偏误--既不敷衍附和，也不盲目反驳：
+
+- **替代解释**：反馈指出的问题，有没有别的原因能解释？（如"正常行为/另一模块引起/误读上下文"）
+- **证据充分性**：反馈依据的证据是否充分？是否断章取义？
+- **主代理实证优先**：对反馈中的事实性断言（"某接口没被调用""某行代码是 X"），用 Read/Grep 自己核实，铁证面前直接确认或反驳，不依赖对方判断
+
+**反馈有误时**：用铁证（代码 file:line/测试输出）技术性反驳，展示证明。判断性分歧无法用铁证决断时，标"待验证"并说明分歧点，不强行采纳也不强行驳回。
+
+> 本段提炼自 icode-skill `references/adversarial.md`「主代理实证 ≠ 自演」与「诚实降级」思想，适配到"接收反馈"场景。
+
+## 底线
+
+**外部反馈 = 待评估的建议，不是必须执行的命令。**
+
+验证。质疑。然后实施。
+
+不要敷衍附和。始终保持技术严谨。
diff --git a/requesting-code-review/SKILL.md b/requesting-code-review/SKILL.md
index feed2d2..712f2fe 100644
--- a/requesting-code-review/SKILL.md
+++ b/requesting-code-review/SKILL.md
@@ -10,7 +10,7 @@ metadata:
 
 # 请求代码审查
 
-派遣代码审查子代理，在问题扩散之前发现它们。审查者获得的是精心组织的评估上下文——绝不是你的会话历史。
+派遣代码审查子代理，在问题扩散之前发现它们。审查者获得的是精心组织的评估上下文——绝不是你的会话历史。这样可以让审查者专注于工作成果而非你的思考过程，同时保留你自己的上下文以便继续工作。
 
 **核心原则：** 早审查，勤审查。
 
@@ -28,21 +28,38 @@ metadata:
 
 ## 如何请求
 
-**1. 获取 git SHA：**
+### 审查范围与多 Git 根
+
+开始审查前，先从用户需求、计划以及 `git status`/diff 确定全部改动路径。对每个改动路径执行：
+
+```bash
+git -C <改动文件所在目录> rev-parse --show-toplevel
+```
+
+- 每个不同结果都是独立 Git 根，分别计算 BASE/HEAD、读取状态和生成 diff。
+- 父仓库 diff 为空不代表嵌套仓库或 submodule 没有改动。
+- 未提交和已暂存改动必须与提交区间一起交给审查者。
+- 不能建立可靠基线的 Git 根标为“待验证”，不得静默漏审。
+
+**1. 确定审查基线：**
 ```bash
-BASE_SHA=$(git rev-parse HEAD~1)  # 或 origin/main
+# 任务审查：使用任务开始前记录的 BASE_SHA
+# 整分支审查：使用与目标分支的 merge-base
+TARGET_BRANCH=<目标分支，如 master 或 origin/main>
+BASE_SHA=$(git merge-base "$TARGET_BRANCH" HEAD)
 HEAD_SHA=$(git rev-parse HEAD)
 ```
 
+**禁止默认使用 `HEAD~1`**：一个任务可能包含多个提交，`HEAD~1` 会静默漏审更早的改动。未提交改动应额外纳入 `git diff` / `git diff --cached`，不能只审查提交区间。
+
 **2. 派遣代码审查子代理：**
 
-使用 Task 工具，指定 `general-purpose` 类型，填写 `code-reviewer.md` 中的模板
+使用当前平台实际提供的子代理工具，填写 `code-reviewer.md` 中的模板。Codex 使用 `spawn_agent`；任务简报已自包含时使用 `fork_turns="none"`，避免继承无关会话历史。当前会话没有子代理能力时，按下方降级路径执行。
 
 **占位符说明：**
 - `{DESCRIPTION}` - 你刚完成的内容简要说明
 - `{PLAN_OR_REQUIREMENTS}` - 预期功能
-- `{BASE_SHA}` - 起始提交
-- `{HEAD_SHA}` - 结束提交
+- `{REVIEW_SCOPES}` - 每个 Git 根的绝对路径、BASE/HEAD、`git status --short`，以及覆盖 committed/staged/unstaged/untracked 改动的审查包或等价 diff
 
 **3. 处理反馈：**
 - Critical 问题立即修复
@@ -63,8 +80,11 @@ HEAD_SHA=$(git rev-parse HEAD)
 [派遣代码审查子代理]
   DESCRIPTION: 添加了 verifyIndex() 和 repairIndex()，支持 4 种问题类型
   PLAN_OR_REQUIREMENTS: docs/superpowers/plans/deployment-plan.md 中的任务 2
-  BASE_SHA: a7981ec
-  HEAD_SHA: 3df7661
+  REVIEW_SCOPES:
+    - Git 根: /path/to/repo
+      Base/Head: a7981ec..3df7661
+      状态: <git status --short 输出>
+      审查包: /path/to/review-a7981ec..3df7661.diff
 
 [子代理返回]:
   优点：架构清晰，测试真实
@@ -77,12 +97,117 @@ HEAD_SHA=$(git rev-parse HEAD)
 [继续任务 3]
 ```
 
-## 常见的合理化借口
+## 与工作流的集成
+
+**子代理驱动开发：**
+- 每个任务完成后审查
+- 在问题叠加之前发现它们
+- 修复后再进入下一个任务
+
+**执行计划：**
+- 每个任务完成后或在自然 checkpoint 审查
+- 获取反馈，应用，继续
+
+**临时开发：**
+- 合并前审查
+- 卡住时审查
+
+
+## 对抗验证增强（可选）
+
+单一审查者存在确认偏误风险（自己委托的审查者，容易顺着你的预期给意见）。对**判断性结论**（"这个设计合理吗""风险够不够"这类无铁证的判断），可追加对抗验证：
+
+**spawn 3 个独立质疑者（必须独立 spawn，不得主代理自演）：**
+
+| 质疑者 | 攻击点 |
+|--------|--------|
+| 证据质疑者 | 引用的证据真在代码/日志里吗？是否断章取义？ |
+| 替代解释者 | 有没有别的原因能解释？（必须提 ≥1 个竞争假设） |
+| 充分性质疑者 | 现有证据够不够定论？还缺什么？ |
+
+**裁决优先级（按顺序判定，命中即定）：**
+1. `refuted`：≥2 质疑者判推翻，或替代解释者给出被证据支撑的竞争解释 -> 撤销
+2. `needs_more_evidence`：充分性质疑者判证据不足 -> 降级待验证（一票否决）
+3. `confirmed`：≥2 质疑者判成立 -> 保留
+4. 兜底：三态分裂/未收敛 -> 降级待验证，如实记录"对抗未收敛"
+
+**诚实降级（硬约束）：** 对抗未收敛时**不得和稀泥默认 confirmed**，必须降级为 `needs_more_evidence` 并如实记录。宁可承认"这条没验成"，也不伪造共识。
+
+**主代理实证 ≠ 自演：** 主代理用 Read/Grep 查到的代码/日志铁证（如"某函数签名确实是 X"）属事实核查，可直接 confirmed 无需对抗。**必须独立对抗的**是判断性结论--无铁证、依赖独立视角的判断，主代理自演无效。
+
+**显式等待（防“未等子代理返回就降级”）：** spawn 质疑者后必须等待 verdict 返回才能进入裁决。一次等待调用超时只表示“本次等待未收到新消息”，**不表示原子代理已失败**；先查询原 agent 状态并继续等待，禁止因此重复 spawn 一个仍在运行的任务。区分四种状态：
 
-| 借口 | 现实 |
+| 状态 | 含义 |
 |------|------|
-| "我自己看一下 diff 就行了，不用专门派审查者" | 你是协调者——在自己的会话里读 diff 会烧掉你继续推进工作所需的上下文窗口。派一个审查子智能体：diff 和评估过程都待在它的上下文里，只有结论回到你这里。 |
-| "审查者需要我的全部会话历史才能理解这次改动" | 给它精心组织的上下文，绝不给会话历史。这样审查者才会盯着工作成果，而不是你的思考过程。 |
+| `sync_ok` | 原 agent 正常返回 verdict |
+| `wait_continued` | 等待调用超时，但原 agent 仍运行，已继续等待 |
+| `replacement_used` | 原 agent 已明确失败/终止或最终输出不可用，启动 1 次替代任务 |
+| `env_no_spawn` | 环境结构性无 spawn 工具（见下方降级路径） |
+
+**子代理失败处理（最多 1 个替代任务，绝不制造重复并发）：**
+1. 原 agent 仍为 running/pending → 继续等待，不 spawn 替代任务
+2. 原 agent 已明确 failed/cancelled/terminated，或最终输出为空、截断、无裁决 → 换措辞后启动 1 个替代任务；仅使用当前接口实际支持的模型或推理配置
+3. 替代任务仍失败 → 诚实降级为 `[未验证-子代理对抗失败]`，计入待验证清单，绝不改由主代理自演裁决
+
+**环境无 spawn 工具的降级路径：** 当当前会话结构性无子代理 spawn 能力（工具未暴露、调用明确返回不支持，或会话类型不支持子代理）时，**不重试、不自演**，走以下降级路径：
+
+1. **诚实降级**：标 `[未验证-环境无spawn工具]`，**非** `[未验证-子代理对抗失败]`（区分结构性不可用 vs 临时失败）
+2. **不重试**：环境无工具，重试无意义
+3. **主代理代行（有条件，不同于自演）**：环境结构性无 spawn 工具是**唯一允许**主代理代行的场景。代行时必须：
+   - 三个视角的推理各自独立（不得"证据也没问题、替代也没发现竞争解释、充分性也够了"这种一致假共识）
+   - 证据质疑者：核对至少 3 条证据指针的原文（Read 实证）
+   - 替代解释者：提至少 1 个竞争解释
+   - 充分性质疑者：列出至少 1 条"还缺的证据"
+   - 标 `[未验证-环境无spawn工具]` 诚实告知用户
+4. **有 spawn 工具时硬禁止代行**：即使部分子代理未返回（如 1/3 返回、2/3 超时），未返回的应按失败处理链路降级，**不得"补齐"为主代理代行**
+
+**Codex CLI 判定方法**：以当前会话是否实际暴露且可调用 `spawn_agent` 为准；配置文件只能作为排障线索，不能单独判定环境无 spawn 工具。
+
+> 本段提炼自 icode-skill 的等待和诚实降级思想，并按 Codex 的实际 agent 生命周期修正：等待超时不等于任务失败，只有明确失败后才允许启动替代任务。
+
+## 多层审查增强（可选）
+
+单层审查（一个 reviewer 看完 diff 给意见）容易遗漏深层问题。对重要变更，可使用三阶段递进审查，每阶段有独立目标，逐层加深：
+
+### 第 1 层·逆推（Reverse）
+
+**不看计划**，只读最终代码，从代码逆推"它在实现什么需求"，再与实际计划/需求对比。
+
+- 目标：发现"代码做了计划没写的、计划写了代码没做的"差异
+- 操作：读代码 -> 逆推需求规格 -> 与计划逐项对比 -> 列出差异清单
+- 价值：不受计划锚定，纯粹从产物倒推，能发现计划盲区
+
+### 第 2 层·固定维度（Fixed）
+
+对照计划 + 最新代码，逐项覆盖以下固定审查维度：
+
+| # | 维度 | 检查问题 |
+|---|------|---------|
+| 1 | 逻辑合理性 | 代码逻辑是否自洽？流程是否闭环？ |
+| 2 | 流程完整性 | 正常路径 + 异常路径 + 边界路径是否全覆盖？ |
+| 3 | 场景覆盖度 | 空入参/超长输入/并发/重入/资源耗尽等场景是否处理？ |
+| 4 | 风险遗漏 | 是否有未识别的风险（安全/性能/兼容性/数据丢失）？ |
+| 5 | 落地可行性 | 接口签名/数据结构/依赖关系是否可编译可运行？ |
+| 6 | 现有实现对照 | 是否存在等价行为？命中处是否已追到调用链和实际行为？应复用、补充还是确需新增？ |
+| 7 | 跨文件一致性 | 接口变更是否全链路同步？上下游数据结构是否对齐？ |
+
+### 第 3 层·自由探索（Free）
+
+不受固定维度限制，从以下角度自由审查，重点是前两层没覆盖的盲区：
+
+- 安全漏洞 / 并发与重入安全 / 资源与内存管理
+- 性能热点 / 可测试性 / 可维护性
+- 编译器/构建兼容 / 跨平台与移植 / API/ABI 兼容
+- 注释完备性与一致性 / 日志覆盖
+
+### 使用建议
+
+- **小变更**：仅第 1 层（逆推）即可--快速发现"做了没做对"
+- **中等变更**：第 1 + 2 层--逆推 + 固定维度覆盖
+- **重要变更**（合并到 main / 核心模块改动）：三层全跑
+- 三层发现的问题统一汇总，按 Critical/Important/Minor 分级处理
+
+> 本段提炼自 icode-skill `steps/05_deepcheck.md`「三阶段递进复检」机制（Reverse -> Fixed -> Free），原规则在 icode 步骤 5 执行，依赖 03_plan_final.md / metadata 字段。此处泛化去除 icode 专属概念，保留三阶段递进结构与核心维度。
 
 ## 红线
 
diff --git a/requesting-code-review/code-reviewer.md b/requesting-code-review/code-reviewer.md
index 08bf9c4..7fcfd70 100644
--- a/requesting-code-review/code-reviewer.md
+++ b/requesting-code-review/code-reviewer.md
@@ -5,7 +5,7 @@
 **用途：** 在工作成果扩散到更多工作之前，对照需求和代码质量标准做一次审查。
 
 ```
-Task tool（general-purpose）:
+当前平台的代码审查子代理（Codex 使用 `spawn_agent`；任务包自包含时使用 `fork_turns="none"`）：
   description: "审查代码改动"
   prompt: |
     你是一名资深代码审查员，精通软件架构、设计模式与最佳实践。
@@ -19,15 +19,13 @@ Task tool（general-purpose）:
 
     {PLAN_OR_REQUIREMENTS}
 
-    ## 待审查的 Git 范围
+    ## 待审查范围
 
-    **Base：** {BASE_SHA}
-    **Head：** {HEAD_SHA}
+    {REVIEW_SCOPES}
 
-    ```bash
-    git diff --stat {BASE_SHA}..{HEAD_SHA}
-    git diff {BASE_SHA}..{HEAD_SHA}
-    ```
+    每个 scope 必须给出 Git 根、BASE/HEAD、工作区状态，以及覆盖 committed、
+    staged、unstaged、untracked 改动的审查包或等价 diff。逐个 scope 审查；
+    父仓库干净不能替代嵌套 Git 根证据。缺少任一范围时标为待验证，不能静默略过。
 
     ## 检查内容
 
@@ -121,8 +119,7 @@ Task tool（general-purpose）:
 **占位符说明：**
 - `{DESCRIPTION}` —— 已构建内容的简要说明
 - `{PLAN_OR_REQUIREMENTS}` —— 预期功能（计划文件路径、任务文本或需求）
-- `{BASE_SHA}` —— 起始 commit
-- `{HEAD_SHA}` —— 结束 commit
+- `{REVIEW_SCOPES}` —— 每个 Git 根的路径、基线、状态和完整审查材料
 
 **审查员返回：** 优点、问题（Critical / Important / Minor）、建议、评估
 
diff --git a/subagent-driven-development/SKILL.md b/subagent-driven-development/SKILL.md
index e270f3f..6a9c653 100644
--- a/subagent-driven-development/SKILL.md
+++ b/subagent-driven-development/SKILL.md
@@ -20,6 +20,8 @@ metadata:
 
 **持续执行：** 不要在任务之间停下来向你的人类伙伴确认。不间断地执行计划里的所有任务。唯一该停下的理由是：你无法解决的 BLOCKED 状态、确实妨碍推进的歧义，或所有任务已完成。"我该继续吗？"之类的询问和进度小结都在浪费他们的时间——他们让你执行计划，那就执行。
 
+**本地授权边界：** 用户和项目指令优先。未经明确授权，不得创建 worktree、修改或新增测试、commit/push，或为了执行本技能而自行启用子智能体；没有子智能体能力时采用等价的内联审查与验证，并如实说明边界。
+
 ## 何时使用
 
 ```dot
diff --git a/subagent-driven-development/implementer-prompt.md b/subagent-driven-development/implementer-prompt.md
index 8b66d7e..28de349 100644
--- a/subagent-driven-development/implementer-prompt.md
+++ b/subagent-driven-development/implementer-prompt.md
@@ -3,7 +3,7 @@
 分派实现子智能体时使用此模板。
 
 ```
-Subagent (general-purpose):
+Subagent（使用当前平台的子代理工具；Codex 的任务简报自包含时设置 `fork_turns="none"`）：
   description: "实现任务 N：[任务名称]"
   model: [模型 —— 必填：按 SKILL.md 的"模型选择"来选；省略模型会默默
          继承会话里最贵的那个]
@@ -35,7 +35,7 @@ Subagent (general-purpose):
     1. 严格按照任务指定的内容实现
     2. 编写测试（如果任务要求则遵循 TDD）
     3. 验证实现是否正常工作
-    4. 提交你的工作
+    4. 只有任务简报明确写明用户已授权提交时，才提交你的工作
     5. 自审（见下文）
     6. 汇报
 
@@ -44,8 +44,8 @@ Subagent (general-purpose):
     **工作过程中：** 如果遇到意料之外或不清楚的情况，**提问**。
     随时可以暂停并澄清。不要猜测或做假设。
 
-    迭代过程中，只跑你正在改动的那部分的聚焦测试；在提交前跑一次
-    完整测试套件，而不是每次编辑后都跑。
+    迭代过程中优先运行覆盖当前改动的现有验证；任务要求完整测试套件时，
+    在交付前跑一次，而不是每次编辑后都跑。未明确要求时不得新增或修改测试文件。
 
     ## 代码组织
 
@@ -106,11 +106,9 @@ Subagent (general-purpose):
 
     ## 审查发现之后
 
-    如果任务审查发现了问题，你会被带着那些发现重新唤起（resume）。
-    修复它们，重跑覆盖被改动代码的测试，然后往你的报告文件里追加一份
-    修复报告：你改了什么、你跑了哪些覆盖用的测试、命令是什么、输出是什么。
-    审查者不会替你重跑测试——你的报告就是测试证据。然后用与第一份报告
-    相同的那个简短状态契约回复。
+    如果审查者发现了问题、你也修复了，就重跑覆盖被改动代码的测试，
+    并把结果追加到你的报告文件里。审查者不会替你重跑测试——
+    你的报告就是测试证据。
 
     ## 报告格式
 
@@ -126,7 +124,7 @@ Subagent (general-purpose):
 
     然后只汇报以下内容（不超过 15 行——细节都在报告文件里）：
     - **状态：** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
-    - 创建的提交（短 SHA + 标题）
+    - 创建的提交（仅已授权并实际提交时填写；否则写“未授权提交，保留工作区改动”）
     - 一行测试小结（例如"14/14 通过，输出干净"）
     - 你的疑虑，如果有
     - 报告文件路径
@@ -138,3 +136,4 @@ Subagent (general-purpose):
     如果你无法完成任务，使用 BLOCKED。如果你需要未提供的信息，
     使用 NEEDS_CONTEXT。绝不默默产出你不确定的工作。
 ```
+</content>
diff --git a/subagent-driven-development/scripts/review-package b/subagent-driven-development/scripts/review-package
index 31852e2..8498719 100755
--- a/subagent-driven-development/scripts/review-package
+++ b/subagent-driven-development/scripts/review-package
@@ -1,45 +1,66 @@
 #!/usr/bin/env bash
-# Generate a review package: commit list, stat summary, and the net
-# diff with extended context, written to a file the reviewer reads in one
-# call. Using the recorded per-task BASE (not HEAD~1) keeps multi-commit
-# tasks intact.
+# Generate a review package containing commits plus the current staged,
+# unstaged, and untracked worktree state. Using the recorded per-task BASE
+# (not HEAD~1) keeps multi-commit tasks intact; including the worktree keeps
+# uncommitted tasks reviewable when the user did not authorize commits.
 #
-# Usage: review-package PLAN_FILE BASE HEAD [OUTFILE]
-# Default OUTFILE: <repo-root>/.superpowers/sdd/<plan-basename>/review-<base7>..<head7>.diff
+# Usage: review-package BASE HEAD [OUTFILE]
+# Default OUTFILE: <repo-root>/.superpowers/sdd/review-<base7>..<head7>.diff
 # (named per range, so a re-review after fixes gets a distinct fresh file).
 set -euo pipefail
 
-if [ $# -lt 3 ] || [ $# -gt 4 ]; then
-  echo "usage: review-package PLAN_FILE BASE HEAD [OUTFILE]" >&2
+if [ $# -lt 2 ] || [ $# -gt 3 ]; then
+  echo "usage: review-package BASE HEAD [OUTFILE]" >&2
   exit 2
 fi
 
-plan=$1
-base=$2
-head=$3
-[ -f "$plan" ] || { echo "no such plan file: $plan" >&2; exit 2; }
+base=$1
+head=$2
 
 git rev-parse --verify --quiet "$base" >/dev/null || { echo "bad BASE: $base" >&2; exit 2; }
 git rev-parse --verify --quiet "$head" >/dev/null || { echo "bad HEAD: $head" >&2; exit 2; }
 
-if [ $# -eq 4 ]; then
-  out=$4
+resolved_head=$(git rev-parse "$head")
+current_head=$(git rev-parse HEAD)
+if [ "$resolved_head" != "$current_head" ]; then
+  echo "HEAD changed: requested $resolved_head, current $current_head" >&2
+  exit 2
+fi
+
+if [ $# -eq 3 ]; then
+  out=$3
 else
-  dir=$("$(cd "$(dirname "$0")" && pwd)/sdd-workspace" "$plan")
+  dir=$("$(cd "$(dirname "$0")" && pwd)/sdd-workspace")
   out="$dir/review-$(git rev-parse --short "$base")..$(git rev-parse --short "$head").diff"
 fi
 
 {
-  echo "# Review package: ${base}..${head}"
+  echo "# Review package: ${base}..${head} + current worktree"
   echo
   echo "## Commits"
   git log --oneline "${base}..${head}"
   echo
-  echo "## Files changed"
-  git diff --stat "${base}..${head}"
+  echo "## Worktree status"
+  git status --short
+  echo
+  echo "## Tracked files changed since BASE"
+  git diff --stat "$base"
+  echo
+  echo "## Tracked diff since BASE"
+  git diff --binary -U10 "$base"
   echo
-  echo "## Diff"
-  git diff -U10 "${base}..${head}"
+  echo "## Untracked files"
+  while IFS= read -r -d '' file; do
+    printf '\n### %s\n\n' "$file"
+    if [ -d "$file" ]; then
+      echo "Untracked directory or nested Git root; review it as a separate scope."
+      continue
+    fi
+    git diff --no-index --binary -- /dev/null "$file" || {
+      rc=$?
+      [ "$rc" -eq 1 ] || exit "$rc"
+    }
+  done < <(git ls-files --others --exclude-standard -z)
 } > "$out"
 
 commits=$(git rev-list --count "${base}..${head}")
diff --git a/subagent-driven-development/task-reviewer-prompt.md b/subagent-driven-development/task-reviewer-prompt.md
index c42a786..1f3bea1 100644
--- a/subagent-driven-development/task-reviewer-prompt.md
+++ b/subagent-driven-development/task-reviewer-prompt.md
@@ -6,7 +6,7 @@
 **目的：** 核实一个任务的实现与其需求匹配（不多不少）且构建良好（整洁、有测试、可维护）
 
 ```
-Subagent (general-purpose):
+Subagent（使用当前平台的子代理工具；Codex 的审查包自包含时设置 `fork_turns="none"`）：
   description: "审查任务 N（规格 + 质量）"
   model: [模型 —— 必填：按 SKILL.md 的"模型选择"来选；省略模型会默默
          继承会话里最贵的那个]
@@ -32,12 +32,14 @@ Subagent (general-purpose):
     **Head：** [HEAD_SHA]
     **Diff 文件：** [DIFF_FILE]
 
-    一次性读取这个 diff 文件——它包含提交列表、stat 摘要，以及
-    带上下文的完整 diff，它就是你对本次改动的视图。diff 的上下文行
+    一次性读取这个 diff 文件——它包含提交列表、工作区状态、从 BASE
+    到当前工作区的 tracked diff，以及未跟踪文件内容/独立 scope 提示。
+    它就是你对本次改动的视图。diff 的上下文行
     **就是**那些被改动的文件：不要单独去 Read 某个被改动的文件，除非
     你必须判断的某个 hunk 在函数中途被截断——并在报告中说明这一点。
-    不要重跑 git 命令。如果 diff 文件缺失，就自己取 diff：
-    `git diff --stat [BASE_SHA]..[HEAD_SHA]` 和 `git diff [BASE_SHA]..[HEAD_SHA]`。
+    不要重跑 git 命令。如果 diff 文件缺失，就自己取 `git status --short`、
+    `git diff --stat [BASE_SHA]`、`git diff [BASE_SHA]` 和未跟踪文件清单；
+    不能退回只看 `[BASE_SHA]..[HEAD_SHA]`。
     不要爬取更广的代码库。只有为了评估一个你能点名的具体风险，才去
     查看 diff 之外的代码——每个点名的风险做一次聚焦检查，并在报告中
     同时点名这个风险和你检查了什么。横切改动是正当的、可点名的风险：
@@ -57,7 +59,7 @@ Subagent (general-purpose):
 
     ## 测试
 
-    实现者已经跑过测试，并为正是这份代码报告了带 TDD 证据的结果。
+    实现者应按任务授权运行验证；只有任务明确要求 TDD 时，报告才应包含 TDD 证据。
     不要为了确认他们的报告而重跑测试套件。只有当阅读代码引出一个
     现有任何运行都无法回答的具体疑问时，才去跑测试——而且是聚焦
     测试，绝不是包级套件、竞态检测运行、或反复的/高次数的循环。
@@ -158,8 +160,12 @@ Subagent (general-purpose):
 - `[BASE_SHA]` —— 本任务之前的提交
 - `[HEAD_SHA]` —— 当前提交
 - `[DIFF_FILE]` —— 必填：控制者写入审查包的那个路径
-  （`scripts/review-package PLAN_FILE BASE HEAD` 会打印它写入的唯一路径；
+  （`scripts/review-package BASE HEAD` 会打印它写入的唯一路径；
   审查包永远不会进入控制者的上下文）
 
 **审查者返回：** 规格合规性结论（✅/❌/⚠️）、优点、问题
 （关键/重要/次要）、任务质量结论
+
+一次修复分派可以同时处理规格差距和质量发现；修复后的重新审查
+覆盖两个结论。
+</content>
diff --git a/systematic-debugging/SKILL.md b/systematic-debugging/SKILL.md
index 2be7448..8588749 100644
--- a/systematic-debugging/SKILL.md
+++ b/systematic-debugging/SKILL.md
@@ -12,6 +12,8 @@ metadata:
 
 ## 概述
 
+随意修复既浪费时间又会引入新 bug。草率的补丁只会掩盖深层问题。
+
 **核心原则：** 在尝试修复之前，务必先找到根本原因。只修症状就是失败。
 
 **敷衍走流程等于违背调试的精神。**
@@ -154,17 +156,46 @@ metadata:
    - 写下来
    - 要具体，不要含糊
 
-2. **最小化测试**
+2. **假设对抗三问（强制必答，缺项视为流程不合规）**
+
+   提出假设后、动手验证前，必须当场回答以下三问。每问须实质作答，不得空泛、不得跳过。未呈现三问回答即进入最小化测试，视为流程不合规。
+
+   > 本规则继承 icode 工作流「对抗质疑三问」的反确认偏误精神，从"质疑历史工单"改造为"质疑当前调试假设"，去掉对 verdict/工单系统的依赖。
+
+   **① 该假设的前提证据是否实证成立？**
+   - 假设依赖的那条日志片段、那个代码行为、那个变量值，是否实际读到了原文？
+   - 禁止凭印象/经验/"应该是"推断，须 Grep/Read 实证引用出处。
+   - 日志分析场景：引用的日志片段必须能 Grep 回原文确认，不得凭记忆转述。
+
+   **证据可靠性分级（强制标明）：**
+   - **直接观察：** 原始日志行、实际运行输出、`git show <hash>:<file>`、二进制内容等可复查原文；可作为根因证据。
+   - **间接推断：** 文件 mtime、空计数结果、经验或语义推测；必须写出推断链与不确定性，不能单独定根因。
+   - 间接推断至少应由一条直接观察交叉验证；例如 mtime 不能证明部署产物内容，空的 `grep -c` 输出也不能直接当作 0 命中。
+
+   **② 有没有反对该假设的证据被忽略了？**
+   - 主动找反证：日志里有没有不吻合该假设的症状？其他组件有没有更合理的解释？
+   - 默认怀疑：没找到反证 ≠ 假设成立。找不到反证要说明"已查 X 处未见矛盾"，不能直接跳过。
+   - 日志分析场景：禁止只捞出支持假设的片段，须检查同时段/同链路是否有矛盾日志被跳过。
+
+   **③ 这个根因是"已证实"还是"只是当前最像"？**
+   - 排除其他可能前不能定论。列出至少 1 个替代假设，并说明为何排除。
+   - 若列不出替代假设，说明对问题理解仍不充分，回到第一阶段补充证据。
+
+   **多候选未区分时先诊断：** 若两个或以上候选根因都能解释现象、现有证据尚不能区分，下一步必须先做能一票区分候选的最小验证，而非按“当前最像”的候选直接实施修复。优先复用现有日志、状态或复现条件；确需新增诊断代码时，先说明最小修改、预期成立/证伪信号，并遵守用户对修改范围的授权。
+
+   **不合规处理：** 三问任一未实质作答，不得进入第 3 步最小化测试，须补齐后再继续。
+
+3. **最小化测试**
    - 做出最小的改动来验证假设
    - 每次只改一个变量
    - 不要同时修复多个问题
 
-3. **继续之前先验证**
-   - 生效了？是 → 进入第四阶段
-   - 没生效？提出新假设
+4. **继续之前先验证**
+   - 生效了？是 -> 进入第四阶段
+   - 没生效？提出新假设（新假设仍须经第 2 步对抗三问）
    - 不要在上面叠加更多修复
 
-4. **当你不确定时**
+5. **当你不确定时**
    - 说"我不理解 X"
    - 不要假装自己知道
    - 寻求帮助
@@ -174,12 +198,10 @@ metadata:
 
 **修复根本原因，而非症状：**
 
-1. **创建失败的测试用例**
-   - 最简化的复现
-   - 尽可能用自动化测试
-   - 没有测试框架就写一次性测试脚本
-   - 修复前必须先有测试
-   - 使用 `superpowers:test-driven-development` 技能来编写规范的失败测试
+1. **确认验证方式（测试以用户明确要求为前提）**
+   - 用户明确要求“写单测”“补充测试用例”或“完善测试覆盖”时：创建最小失败测试，并使用 `superpowers:test-driven-development` 技能完成红绿验证
+   - 未明确要求时：不得自动新增或修改 `test/` 下的测试代码，也不得把“先写失败测试”作为修复前置条件
+   - 此时优先复用已有测试、构建、静态检查或用户提供的复现路径，记录本次采用的验证边界
 
 2. **实施单一修复**
    - 修复已定位的根本原因
@@ -187,11 +209,27 @@ metadata:
    - 不做"顺便改改"的优化
    - 不捆绑重构
 
+   **修复方案三档分级（防"宁多勿少"）：** Bug 修复时，方案必须分 A/B/C 三档呈现，不得平铺：
+
+   | 档位 | 含义 | 判定问题 |
+   |------|------|---------|
+   | **A·根因修复**（Must-fix） | 根因触发层的最小修复 | "不改会复现吗？"->是 |
+   | **B·兜底防御**（Defensive） | 增强健壮性，不改根因也能工作 | "改了更健壮但不改根因也能工作？"->是 |
+   | **C·后续工单**（Out-of-scope） | 与本次根因不直接相关 | "与本次根因不直接相关？"->是 |
+
+   - **最小修复 = 只做 A 档**（只做必改项）
+   - B 档须标注触发条件 + "A 修复后触发概率"；安全停止类 B 档即使"A 修复后不触发"也需用户显式确认才能降级为 C
+   - C 档进范围外，另开工单/issue，不混入主方案
+   - 机制层修复按"不改会复现吗"归 A/B，不自动归 B
+   - 跨工程的 A 档须标注"跨工程：属 \<X\>，本工程不改，已转工单"
+   - **适用范围**：bug 修复强制；纯功能开发可参考分档但非强制
+
+   > 本规则治反偷懒机制的副作用--"宁多勿少"：为避免漏改，倾向把根因修复 + 兜底 + 后续优化全部平铺，淹没真正根因修复。分档后让"必改项"与"锦上添花项"清晰分离。
+
 3. **验证修复**
-   - 测试现在通过了吗？
-   - 其他测试没有被破坏吧？
-   - 问题真的解决了吗？
-   - 宣称成功之前，使用 `superpowers:verification-before-completion` 技能
+   - 已有测试现在通过了吗？（有可运行测试时）
+   - 构建、静态检查或用户提供的复现路径是否证明问题已解决？
+   - 本次未写测试时，是否已明确说明未覆盖的验证边界？
 
 4. **如果修复不起作用**
    - 停下来
@@ -224,6 +262,7 @@ metadata:
 - "一次性改多个地方，跑测试看看"
 - "跳过测试，我手动验证"
 - "大概是 X 的问题，让我修一下"
+- "假设我已有把握，直接验证"（跳过第三阶段对抗三问）
 - "我不完全理解，但这应该能行"
 - "模式说的是 X，但我换个方式用"
 - "主要问题有这些：[未经调查就列出修复方案]"
@@ -265,7 +304,7 @@ metadata:
 |------|---------|---------|
 | **1. 根因** | 阅读错误、复现、检查变更、收集证据 | 理解了什么出了问题以及为什么 |
 | **2. 模式** | 找到正常示例、对比 | 识别出差异 |
-| **3. 假设** | 提出理论、最小化验证 | 假设被验证或产生新假设 |
+| **3. 假设** | 提出假设、对抗三问、最小化验证 | 假设被验证或产生新假设 |
 | **4. 实施** | 创建测试、修复、验证 | bug 已修复，测试通过 |
 
 ## 当流程显示"找不到根因"
@@ -287,3 +326,14 @@ metadata:
 - **`defense-in-depth.md`** - 找到根因后，在多个层级添加校验
 - **`condition-based-waiting.md`** - 用条件轮询替代硬编码等待时间
 
+**相关技能：**
+- **superpowers:test-driven-development** - 用于创建失败测试用例（第四阶段，第 1 步）
+- **superpowers:verification-before-completion** - 在宣称成功之前验证修复确实有效
+
+## 实际效果
+
+调试实践中的数据：
+- 系统化方法：15-30 分钟修复
+- 随意修复方法：2-3 小时反复折腾
+- 一次修复成功率：95% vs 40%
+- 引入新 bug：几乎为零 vs 经常发生
diff --git a/test-driven-development/SKILL.md b/test-driven-development/SKILL.md
index 213e299..b8e9625 100644
--- a/test-driven-development/SKILL.md
+++ b/test-driven-development/SKILL.md
@@ -1,6 +1,6 @@
 ---
 name: test-driven-development
-description: 在实现任何功能或修复 bug 时使用，在编写实现代码之前
+description: 仅当用户明确要求编写单元测试、补充测试用例、完善测试覆盖时使用；功能开发与代码改造流程中不自动触发，不编写失败测试或 TDD 前置测试
 version: "1.0.0"
 license: MIT
 metadata:
diff --git a/using-git-worktrees/SKILL.md b/using-git-worktrees/SKILL.md
index be0c82a..6f0ef97 100644
--- a/using-git-worktrees/SKILL.md
+++ b/using-git-worktrees/SKILL.md
@@ -91,7 +91,7 @@ git rev-parse --show-superproject-working-tree 2>/dev/null
 git check-ignore -q .worktrees 2>/dev/null || git check-ignore -q worktrees 2>/dev/null
 ```
 
-**如果未被忽略：** 添加到 .gitignore，提交该改动，然后继续。
+**如果未被忽略：** 停止并向用户说明需要修改忽略规则。只有用户授权后才修改 `.gitignore` 或本地 exclude；commit 需要单独的明确授权。忽略校验通过前不得创建项目内 worktree。
 
 **为什么关键：** 防止 worktree 内容被意外提交到仓库。
 
@@ -105,7 +105,7 @@ git worktree add "$path" -b "$BRANCH_NAME"
 cd "$path"
 ```
 
-**沙盒回退：** 如果 `git worktree add` 因权限错误（沙盒拒绝）失败，告诉用户沙盒阻止了 worktree 创建，你将在当前目录原地工作。然后原地运行 setup 和基线测试。
+**创建失败：** 如果 `git worktree add` 因权限、路径或环境限制失败，停止并报告原始错误。只有用户明确同意改为当前目录实现后，才能原地运行 setup 和基线验证；不得自动扩大到原工作区。
 
 ## 步骤 2：项目设置
 
diff --git a/using-superpowers/SKILL.md b/using-superpowers/SKILL.md
index ecd29e4..aad87fe 100644
--- a/using-superpowers/SKILL.md
+++ b/using-superpowers/SKILL.md
@@ -1,6 +1,6 @@
 ---
 name: using-superpowers
-description: 在开始任何对话时使用——确立如何查找和使用技能，要求在任何响应（包括澄清性问题）之前调用 Skill 工具
+description: 在开始任何对话时使用——确立如何查找和使用技能，要求在任何响应（包括澄清性问题）之前调用相关技能
 version: "1.0.0"
 license: MIT
 metadata:
@@ -24,7 +24,7 @@ metadata:
 
 **在任何响应或操作之前调用相关或被请求的技能**——包括澄清性问题、探索代码库、或查看文件之前。如果调用后发现技能不适合当前情况，你不需要使用它。
 
-**在进入 EnterPlanMode 之前：** 如果你还没有头脑风暴过，先调用头脑风暴技能。
+**在进入计划模式之前：** 如果你还没有头脑风暴过，先调用头脑风暴技能。
 
 然后宣布"使用 [技能] 来 [目的]"，并严格遵循该技能。如果它有检查清单，为每个条目创建一个待办。
 
@@ -35,6 +35,20 @@ metadata:
 - "让我们构建 X" → 先用 superpowers:brainstorming，再用实现技能。
 - "修复这个 bug" → 先用 superpowers:systematic-debugging，再用领域技能。
 
+## 需求文档与设计规格评审路由
+
+当用户要求分析、评审、完善需求文档，或确认设计是否满足落地条件时，**必需子技能：** 使用 `brainstorming` 进行规格收敛。`chinese-documentation` 仅负责最终中文排版，`chinese-code-review` 仅可作为最终审查的表达层，不得替代规格收敛流程。
+
+评审发现必须先分流，再决定是否询问用户：
+
+| 类型 | 处理方式 |
+|------|---------|
+| 技术确定项 | 通过现有代码、协议或文档查证并自行补齐，不得转交用户裁决；评审仅授权只读时给出明确结论，已授权修改时直接回填文档。 |
+| 产品裁决项 | 仅保留确实存在多种产品行为且现有证据无法确定的事项；给出推荐方案和影响，每次只讨论一个。 |
+| 外部依赖或后续能力 | 记录前置条件、当前边界和扩展锚点；不影响当前版本时不得阻塞收敛。 |
+
+**不得只列出问题清单后停止，等待用户追问“下一步怎么做”。** 有产品裁决项时，在同一轮评审结论后主动进入第一个裁决项；没有产品裁决项时，根据当前授权继续更新文档或给出明确的落地结论。
+
 ## 红线
 
 这些想法意味着停下——你在合理化：
@@ -54,6 +68,62 @@ metadata:
 | "这样做感觉很高效" | 无纪律的行动浪费时间。技能防止这一点。 |
 | "我知道那是什么意思" | 知道概念 ≠ 使用技能。调用它。 |
 
+
+## 执行反偷懒约束
+
+调用技能后，执行过程中禁止以下偷工减料（一经发现，该步骤产出视为不合规）：
+
+| 偷懒行为 | 合规要求 |
+|---------|---------|
+| 合并产出：把多个独立产物揉进同一文件 | 每个产物独立成文 |
+| 跳过子项：技能要求的子项/维度/章节写"略""同上" | 完整覆盖所有子项 |
+| 降低轮次：审查/复检轮次未达标就声称"已无新问题" | 达标或有明确收敛判据 |
+| 伪代码替代实现：用 `// TODO`/`// ...` 占位代替真实代码 | 落地可运行代码 |
+| 审查/验证走过场：结论全是"无问题""通过"无具体证据 | 每条结论附证据回指 |
+| 跳过验证就宣称完成：未运行验证命令就声称通过 | 见 verification-before-completion |
+| 伪造共识/默认通过：证据不足时默认确认、对抗未收敛时和稀泥 | 证据不足标"待验证"，不伪造确认 |
+
+**总则**：禁止以"省 token/省时间/已大致足够"为由跳过技能定义的子项、轮次和验证。完整完成，不得合并、跳过、降级。
+
+> 本段提炼自 icode-skill `references/anti_laziness.md`，泛化去除 icode 专属的步骤/ticket 概念。原文件 16 条偷懒行为详见 icode-skill。
+
+## 工具调用模式规范
+
+解决"每次只发一个工具调用，等结果回来后就停，需要用户手动点继续"的交互问题。**两条硬性规则**：
+
+1. **批量独立调用**：对无依赖、只读或不共享写入目标、且当前工具允许并行的调用，应在同一个回复中批量发出。包括但不限于：
+   - 多个独立文件的 Read
+   - 多个独立目录的 ls 查询
+   - 多个不相关的 grep/ripgrep 搜索
+   - 多个互不依赖的 Bash 命令
+   - 多个独立子任务用子代理工具并行启动
+
+   **例外**：有副作用、共享资源/写入目标、受速率或并发配额限制，或工具明确要求串行/禁止并行的调用，必须串行执行。
+
+   **判定**：A 的结果影响 B 的执行 -> 串行；A 和 B 互不依赖，且满足上述并行前提 -> 在同一个回复中批量并行发出。
+
+2. **连续执行不等待**：拿到工具结果后，在同一回复中立即继续后续步骤/决策，不等用户主动推进。仅以下情况可以停下来问用户：
+   - 需要用户做二元决策（如"复用/新建"）
+   - 不可逆操作需用户确认（如删除/覆盖文件）
+   - 信息不足以推进（如需要用户补充需求细节）
+
+**违规**：对满足并行前提的独立调用无故拆分发送、每次停下来等用户推进后才继续。
+**合规**：对满足并行前提的调用批量发送 + 拿到结果后立即继续。
+
+> 本段提炼自 icode-skill `SKILL.md`「工具调用模式规范」段，泛化去除 icode 专属的 MCP 调用覆盖强制化关联，保留"批量并行 + 连续执行"核心规则。平台中性，Claude Code / Codex CLI 均适用。
+
+## 重要决策记录
+
+重要判断或方案取舍前，先完成结构化分析，但**不得要求输出或暴露模型的内部推理过程**。对用户和后续审查者只保留可复核的决策记录：
+
+- 已确认事实及证据来源
+- 仍待验证的假设或限制
+- 采用的方案、主要取舍和验证方式
+
+当前平台提供思考类工具时可按需使用；工具不可用不构成阻塞，也不要求用显式“思考块”代替。需要跟踪多步骤工作时使用当前平台的计划/待办工具。
+
+> 本段吸收 icode-skill 的结构化分析思想，但去除了强制 MCP 和显式推理留痕要求。
+
 ## 平台适配
 
 如果你的运行环境在下面列出，请阅读对应的参考文件获取特殊说明：
diff --git a/using-superpowers/references/codex-tools.md b/using-superpowers/references/codex-tools.md
index 36c610b..d43b19c 100644
--- a/using-superpowers/references/codex-tools.md
+++ b/using-superpowers/references/codex-tools.md
@@ -7,19 +7,23 @@ Skills 使用 Claude Code 的工具名称。在 Codex 中遇到这些名称时
 | `Task` 工具（派遣子 agent） | `spawn_agent` |
 | 多个 `Task` 调用（并行） | 多个 `spawn_agent` 调用 |
 | Task 返回结果 | `wait_agent` |
-| Task 自动完成 | `close_agent` 释放槽位 |
+| Task 状态检查 | `list_agents`；等待超时不代表子 agent 已失败 |
 | `TodoWrite`（任务跟踪） | `update_plan` |
 | `Skill` 工具（调用 skill） | Skills 原生加载——直接按说明操作 |
 | `Read`、`Write`、`Edit`（文件） | 使用原生文件工具 |
 | `Bash`（执行命令） | 使用原生 shell 工具 |
 
-## 子 Agent 派遣需要多 Agent 支持
+## 子 Agent 派遣
 
-在 Codex 配置文件（`~/.codex/config.toml`）中添加：
+以**当前会话实际暴露且可调用的工具**为准。配置文件只能用于排查“为什么当前会话没有该工具”，不能单独证明子 agent 可用或不可用。
+
+当任务简报已包含完整上下文、且需要隔离上下文时，调用 `spawn_agent` 应使用 `fork_turns="none"`；如果子 agent 必须理解最近对话，才选择最小必要的正数或 `all`。
+
+部分 Codex 版本可通过以下配置启用多 agent：
 
 ```toml
 [features]
 multi_agent = true
 ```
 
-启用后可使用 `spawn_agent`、`wait_agent` 和 `close_agent`，支持 `dispatching-parallel-agents` 和 `subagent-driven-development` 等 skills。使用 subagent-driven-development 时，implementer 和 reviewer 子 agent 完成全部工作后应始终 `close_agent` 释放。
+配置生效后仍需重新启动会话，并检查当前工具面是否实际包含 `spawn_agent`、`wait_agent`、`list_agents` 等接口。只有当前接口确实提供清理/关闭工具时才调用；未提供时不要虚构 `close_agent`。
diff --git a/verification-before-completion/SKILL.md b/verification-before-completion/SKILL.md
index 3338111..50caa16 100644
--- a/verification-before-completion/SKILL.md
+++ b/verification-before-completion/SKILL.md
@@ -12,6 +12,8 @@ metadata:
 
 ## 概述
 
+在没有验证的情况下宣称工作完成，这不是高效，而是不诚实。
+
 **核心原则：** 始终用证据支撑结论。
 
 **对这条规则敷衍了事，就等于违背了它的精神。**
@@ -40,6 +42,19 @@ metadata:
 跳过任何一步 = 说谎，不是验证
 ```
 
+## 证据新鲜度与增量修改
+
+验证证据只对它实际覆盖的那版产物有效。完成验证后又发生任何代码、配置、计划或依赖变化时：
+
+1. 旧证据对受影响结论自动失效，不得继续引用为“已通过”。
+2. 重新读取受影响文件和当前 diff，判断哪些调用链、需求点和验证命令需要重跑。
+3. 至少重跑覆盖新增改动及其上下游影响的验证；无法重跑的部分标“待验证”并说明原因。
+4. 更新交付摘要，明确旧结论是否仍成立；不得只追加“补了一点修改”而沿用旧完成声明。
+
+## 多 Git 根验证边界
+
+对所有改动文件所在目录执行 `git -C <改动文件所在目录> rev-parse --show-toplevel`，按实际 Git 根分别检查 status、diff 和验证结果。父仓库干净不证明嵌套仓库、submodule 或独立模块干净；每个受影响 Git 根都必须单独给出证据或待验证说明。
+
 ## 常见失败模式
 
 | 结论 | 需要 | 不够格 |
@@ -84,12 +99,14 @@ metadata:
 ❌ "应该能通过了" / "看起来对了"
 ```
 
-**回归测试（TDD 红-绿）：**
+**回归测试（仅用户明确要求新增/修改测试时）：**
 ```
 ✅ 编写 → 运行（通过）→ 回退修复 → 运行（必须失败）→ 恢复 → 运行（通过）
 ❌ "我写了回归测试"（没有经过红-绿验证）
 ```
 
+用户未明确要求新增或修改测试时，不创建回归测试；改用现有测试、构建、静态检查或原问题复现路径验证，并如实说明验证边界。
+
 **构建：**
 ```
 ✅ [运行构建] [看到：exit 0] "构建通过"
@@ -108,6 +125,15 @@ metadata:
 ❌ 信任代理报告
 ```
 
+## 为什么这很重要
+
+来自 24 次失败记录：
+- 搭档说"我不信你"——信任被破坏
+- 未定义的函数被交付——会直接崩溃
+- 遗漏需求被交付——功能不完整
+- 虚假完成浪费的时间 → 返工 → 重做
+- 违反原则："诚实是核心价值。如果你说谎，就会被替换。"
+
 ## 何时使用
 
 **以下情况之前必须使用：**
@@ -124,3 +150,60 @@ metadata:
 - 暗示成功
 - 任何传达完成/正确性的沟通
 
+
+## 证据回指纪律
+
+每条"通过/完成/已修复"结论必须能回指到**具体证据片段**：
+
+| 结论类型 | 回指要求 |
+|---------|---------|
+| 测试通过 | 测试命令 + 输出摘要（如"34/34 pass"） |
+| 构建成功 | 构建命令 + exit code |
+| Bug 已修复 | 复现原症状的测试 + 通过输出 |
+| 代码改到位 | 代码 file:line + 关键 diff |
+| 需求已满足 | 逐项核对清单 + 每项证据 |
+
+**做不到回指的结论**：只进"待验证"清单，不写进"已完成"。断言无法实证验证时，标 `[未验证-证据不足]`，**不得默认通过**。
+
+这与"门控函数"一致--门控要求运行命令看输出，回指纪律要求把输出片段锚定到每条结论上，让结论可追溯、可复核。
+
+> 本段提炼自 icode-skill `references/adversarial.md`「证据回指纪律」段，原规则：每条 confirmed 结论必须能回指到具体证据片段（计划章节/代码 file:line/日志节点+时间+原文）。
+
+## 需求收敛三视角
+
+宣称"需求已满足"之前，必须从三个独立视角逐条对照，既防需求遗漏，也防重复实现：
+
+| 视角 | 对照基线 | 检查问题 |
+|------|---------|---------|
+| **A·需求角度** | 用户原始需求（对话/规格/issue 原文） | 用户想要的每一条，最终产物是否满足？ |
+| **B·计划角度** | 实现计划/任务清单 | 计划定义的每个功能点，代码是否落地？ |
+| **C·必要性角度** | 最终产物与工程现有行为 | 新增功能是否已有等价实现？应复用、补充还是确需新增？ |
+
+**操作步骤：**
+
+1. **拆解需求**：重读用户原始需求，逐条拆解为可验证的需求点
+2. **三视角对照**：每个需求点同时从需求、计划和现有实现三个视角给出判定；视角 C 的文本命中必须追到调用链和实际行为
+
+```markdown
+| 需求点 | A·需求角度 | B·计划角度 | C·必要性角度 | 收敛判定 |
+|--------|------------|------------|----------------|---------|
+| <需求 1> | 用户原话 -> 产物满足 ✅ | 计划 §X.Y -> 代码 file:line ✅ | 无等价行为 ✅ | ✅ 收敛 |
+| <需求 2> | 边缘 case 未覆盖 ❌ | 计划 §X.Z -> 代码 file:line ✅ | 部分覆盖，边界已说明 | ❌ 追加修复 |
+| <需求 3> | 用户需求已存在 | 计划遗漏 ❌ | 现有入口已完整覆盖 | ❌ 改为复用/取消新增 |
+```
+
+3. **收敛判定**：三个视角都通过才算收敛；任一未通过 -> 修正计划、改为复用或追加修复任务，不得跳过
+
+**三个视角的区别**：B 检查“计划定义的做了没”，A 检查“用户想要的做了没”，C 检查“做的事情是否本来就已有实现”。三者互不替代。
+
+**与门控函数的关系**：门控函数要求“什么证据能证明结论”，三视角要求“哪些需求点、计划点和必要性需要验证”，把笼统的“需求已满足”拆成可复核的对照表。
+
+> A/B 视角提炼自 icode-skill 原始需求收敛机制；C 视角提炼自 v2.11 `references/necessity_check.md`，均已去除 metadata 和工单状态依赖。
+
+## 底线
+
+**验证没有捷径。**
+
+运行命令。阅读输出。然后才能宣称结果。
+
+这没有商量余地。
diff --git a/writing-plans/SKILL.md b/writing-plans/SKILL.md
index 704209a..956c207 100644
--- a/writing-plans/SKILL.md
+++ b/writing-plans/SKILL.md
@@ -12,16 +12,15 @@ metadata:
 
 ## 概述
 
-编写全面的实现计划，假设工程师对我们的代码库零上下文，且品味存疑。记录他们需要知道的一切：每个任务要修改哪些文件、代码、测试、可能需要查阅的文档、如何测试。将整个计划拆成小步骤任务。DRY。YAGNI。TDD。频繁 commit。
+编写全面的实现计划，假设工程师对我们的代码库零上下文，且品味存疑。记录他们需要知道的一切：每个任务要修改哪些文件、代码、可能需要查阅的文档，以及如何验证。将整个计划拆成小步骤任务。DRY。YAGNI。
 
 假设他们是有经验的开发者，但对我们的工具链和问题领域几乎一无所知。假设他们不太擅长测试设计。
 
 **开始时宣布：** "我正在使用 writing-plans 技能创建实现计划。"
 
-**上下文：** 此技能应在专用 worktree 中运行（由 brainstorming 技能创建）。
+**上下文：** 编写计划本身不要求创建 worktree。进入实现前如需隔离，由 `using-git-worktrees` 按用户授权和项目规则处理。
 
-**计划保存位置：** `docs/superpowers/plans/YYYY-MM-DD-<feature-name>.md`
-- （用户对计划位置的偏好优先于此默认值）
+**计划落盘：** 只有用户要求、项目惯例或已批准流程需要时才保存；默认位置为 `docs/superpowers/plans/YYYY-MM-DD-<feature-name>.md`，用户偏好优先。
 
 ## 范围检查
 
@@ -38,18 +37,14 @@ metadata:
 
 此结构决定了任务分解。每个任务应产出独立的、有意义的变更。
 
-## 任务粒度定界
-
-一个任务是**能独立承载自己那一轮测试循环、且值得一个全新审查者把关**的最小单元。划任务边界时：把搭建、配置、脚手架和文档这些步骤，折进那个真正需要它们的交付物所在的任务里；只在「审查者有可能否掉这个任务、同时批准它旁边那个」的地方才拆开。每个任务都以一个**可独立测试的交付物**结束。
-
 ## 小步骤任务粒度
 
-**每步是一个操作（2-5 分钟）：**
-- "编写失败的测试" - 一步
-- "运行它确认失败" - 一步
-- "实现最少代码让测试通过" - 一步
-- "运行测试确认通过" - 一步
-- "Commit" - 一步
+**每步是一个可独立验证的操作（通常 2-5 分钟）：**
+- 阅读目标符号及调用链 - 一步
+- 完成一个最小实现改动 - 一步
+- 运行与该改动匹配的现有验证命令 - 一步
+- 只有用户明确要求测试时，才加入新增/修改测试及 TDD 步骤
+- 只有用户明确授权提交时，才加入 Commit 步骤
 
 ## 计划文档头部
 
@@ -75,39 +70,31 @@ metadata:
 ### 任务 N：[组件名称]
 
 **文件：**
-- 创建：`exact/path/to/file.py`
 - 修改：`exact/path/to/existing.py:123-145`
-- 测试：`tests/exact/path/to/test.py`
+- 关联：`exact/path/to/caller.py:42`
+- 测试：`tests/exact/path/to/test.py`（仅用户明确要求新增/修改测试时列出）
 
-- [ ] **步骤 1：编写失败的测试**
+- [ ] **步骤 1：核对现有实现和调用链**
 
-```python
-def test_specific_behavior():
-    result = function(input)
-    assert result == expected
+```bash
+rg -n "function_name|related_symbol" exact/path
 ```
 
-- [ ] **步骤 2：运行测试验证失败**
+- [ ] **步骤 2：实施最小修改**
 
-运行：`pytest tests/path/test.py::test_name -v`
-预期：FAIL，报错 "function not defined"
-
-- [ ] **步骤 3：编写最少实现代码**
-
-```python
-def function(input):
-    return expected
+```text
+在这里给出本任务需要落地的精确代码或逐行修改说明；不得使用 TODO 或省略号占位。
 ```
 
-- [ ] **步骤 4：运行测试验证通过**
+- [ ] **步骤 3：运行现有验证**
 
-运行：`pytest tests/path/test.py::test_name -v`
-预期：PASS
+运行：`<项目已有的构建、静态检查、测试或复现命令>`
+预期：`<明确的退出码或输出判据>`
 
-- [ ] **步骤 5：Commit**
+- [ ] **步骤 4：Commit（仅用户已授权时保留）**
 
 ```bash
-git add tests/path/test.py src/path/file.py
+git add exact/path/to/existing.py exact/path/to/caller.py
 git commit -m "feat: add specific feature"
 ```
 ````
@@ -122,6 +109,13 @@ git commit -m "feat: add specific feature"
 - 只描述做什么而不展示怎么做的步骤（代码步骤必须有代码块）
 - 引用了未在任何任务中定义的类型、函数或方法
 
+## 注意事项
+- 始终使用精确的文件路径
+- 每个步骤都包含完整代码——如果步骤涉及代码变更，就展示代码
+- 精确的命令和预期输出
+- DRY、YAGNI
+- 测试和 commit 都服从用户授权边界：未明确要求时不新增/修改测试，不把 commit 写成强制步骤
+
 ## 自检
 
 编写完整计划后，以全新视角审视规格并对照检查计划。这是你自己执行的检查清单——不是子代理调度。
@@ -136,9 +130,9 @@ git commit -m "feat: add specific feature"
 
 ## 执行交接
 
-保存计划后，提供执行选项：
+计划完成后提供执行选项。已落盘时给出路径；未要求落盘时直接以当前对话中的计划为准：
 
-**"计划已完成并保存到 `docs/superpowers/plans/<filename>.md`。两种执行方式：**
+**"计划已完成。两种执行方式：**
 
 **1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行审查，快速迭代
 
diff --git a/writing-plans/plan-document-reviewer-prompt.md b/writing-plans/plan-document-reviewer-prompt.md
index af97df3..4b441d5 100644
--- a/writing-plans/plan-document-reviewer-prompt.md
+++ b/writing-plans/plan-document-reviewer-prompt.md
@@ -7,7 +7,7 @@
 **调度时机：** 完整计划编写完成后。
 
 ```
-Task tool（通用）:
+当前平台的审查子代理（Codex 使用 `spawn_agent`；审查材料自包含时设置 `fork_turns="none"`）：
   description: "审查计划文档"
   prompt: |
     你是一名计划文档审查员。验证此计划是否完整并准备好进行实现。
diff --git a/writing-skills/SKILL.md b/writing-skills/SKILL.md
index 0516a1c..3de1142 100644
--- a/writing-skills/SKILL.md
+++ b/writing-skills/SKILL.md
@@ -14,13 +14,13 @@ metadata:
 
 **编写技能就是将测试驱动开发应用于流程文档。**
 
-**个人技能存放在智能体特定的目录中（Claude Code 用 `~/.claude/skills`，Codex 用 `~/.agents/skills/`）**
+**个人技能存放在智能体特定的目录中（Claude Code 用 `~/.claude/skills`，Codex CLI 通过软链共享同一目录）**
 
 你编写测试用例（带子智能体的压力场景），观察它们失败（基线行为），编写技能（文档），观察测试通过（智能体遵守规则），然后重构（堵住漏洞）。
 
 **核心原则：** 如果你没有观察到智能体在没有该技能时失败，你就不知道这个技能是否教了正确的东西。
 
-**必需背景：** 在使用此技能前，你必须理解 superpowers:test-driven-development。该技能定义了基本的红-绿-重构循环。本技能将 TDD 适配到文档编写中。
+本技能借用“红—绿—重构”作为流程文档的验证模型，但不因此自动触发代码 TDD，也不授权新增/修改项目测试文件。Skill 行为验证优先使用临时提示场景、现有静态检查或临时目录；用户对测试和文件修改的限制始终优先。
 
 **官方指南：** Anthropic 官方的技能编写最佳实践请参见 anthropic-best-practices.md。该文档提供了补充本技能 TDD 导向方法的额外模式和指南。
 
@@ -103,7 +103,7 @@ skills/
 - `description`：第三人称，仅描述何时使用（不是做什么）
   - 以"Use when..."开头，聚焦于触发条件
   - 包含具体的症状、场景和上下文
-  - **绝不总结技能的流程或工作流**（参见 SDO 章节了解原因）
+  - **绝不总结技能的流程或工作流**（参见 CSO 章节了解原因）
   - 尽量控制在 500 字符以内
 
 ```markdown
@@ -141,7 +141,7 @@ description: Use when [具体的触发条件和症状]
 ```
 
 
-## 技能发现优化（SDO）
+## Claude 搜索优化（CSO）
 
 **发现至关重要：** 未来的 Claude 需要找到你的技能
 
@@ -279,7 +279,7 @@ wc -w skills/path/SKILL.md
 - `creating-skills`、`testing-skills`、`debugging-with-logs`
 - 主动的，描述你正在进行的操作
 
-### 5. 交叉引用其他技能
+### 4. 交叉引用其他技能
 
 **编写引用其他技能的文档时：**
 
@@ -460,23 +460,6 @@ pptx/
 
 **以上所有都意味着：部署前测试。无例外。**
 
-## 让形式匹配失败类型
-
-在写指导内容之前，先给基线失败**归类**。能让某一类失败变得无懈可击的形式，用在另一类上会**可测量地反噬**。
-
-| 基线失败 | 正确的形式 | 错误的形式 |
-|---|---|---|
-| 压力之下跳过/违反规则（明知故犯） | 禁令 + 合理化借口表 + 红线（见下方"让技能经受住合理化的考验"） | 软性建议（"优先……"、"考虑……"） |
-| 遵守了，但产出的**形状**不对（提示词臃肿、结论被埋、复述规格） | 正面配方或契约：直接说明产出**是什么** —— 它由哪些部分组成、按什么顺序 | 禁令清单（"不要复述"、"绝不旁白"） |
-| 在他们**本来就会产出**的东西里漏掉了必需元素 | 结构性手段：在他们要填的模板里放一个 REQUIRED 字段或占位槽 | 在模板附近写散文式提醒 |
-| 行为**应当取决于某个条件** | 挂在可观察谓词上的条件句（"如果简报存在，就引用它"） | 无条件规则 + 一堆例外条款 |
-
-**为什么禁令在"塑形"类问题上会反噬：** 在存在竞争性激励时（比如"让提示词自包含"），智能体会**跟"不要 X"讨价还价**。在针对分派提示词指导做的同题措辞对照测试里，禁令组产出的不想要的内容明显**多于**配方组（两组分布完全分离），甚至比"完全不给指导"的对照组还差 —— 请对你自己的场景做微型测试，别想当然，但**永远不要把禁令当默认选择**。配方留不下可讨价还价的空间：产出要么符合所说的形状，要么不符合。
-
-**无论你选哪种形式，都适用的规则：**
-- **不要加"视情况"从句。** "不要 X，除非它很重要"会重新打开谈判 —— 在同一批措辞测试里，给一个胜出的配方追加**一条**"视情况"从句，就把它从稳定退化成了飘忽。真正的例外要表达成**它自己的**条件句，挂在可观察的谓词上。
-- **豁免条款不会限定作用域。** "这条长度限制不适用于代码块"，照样会压制代码块。如果产出里有一部分必须豁免，就**重构结构让规则碰不到它**，而不是写豁免。
-
 ## 让技能经受住合理化的考验
 
 执行纪律的技能（如 TDD）需要抵抗合理化。智能体很聪明，在压力下会找到漏洞。
@@ -543,7 +526,7 @@ pptx/
 **以上所有都意味着：删除代码。用 TDD 重新开始。**
 ```
 
-### 更新 SDO 以包含违规症状
+### 更新 CSO 以包含违规症状
 
 在描述中添加：你即将违反规则时的症状：
 
@@ -574,18 +557,6 @@ description: use when implementing any feature or bugfix, before writing impleme
 
 智能体找到了新的合理化借口？添加明确的反驳。重新测试直到无懈可击。
 
-### 先做措辞微型测试，再跑完整场景
-
-完整的压力场景是最后一道关卡，但它每轮迭代都又慢又贵。先用**微型测试**验证措辞本身：
-
-1. **每次调用一个全新上下文的样本** —— 一次裸 API 调用，或者没有 API 权限时用一个单发子智能体。system prompt 放**这条指导实际会存在的真实上下文**（完整的 skill 或提示词模板，不是把指导单独拎出来）；user message 放一个会**诱发该失败**的任务。
-2. **永远带一个"不给指导"的对照组。** 如果对照组根本没表现出那个失败，那就没什么可修的 —— 停下，别写这条指导。
-3. **每个变体至少 5 次重复。** 单个样本会骗人。
-4. **每一条被标记的命中都要人工读一遍。** 想用程序打分可以，但模板回声和被引用的反例会**伪装成命中**；只看自动计数会同时高估失败和成功。
-5. **方差本身就是一个指标。** 指导真正生效时，多次重复会收敛到同一种形状。5 次重复出现 5 种不同解读，说明这个措辞**没有约束力** —— 先收紧形式，别急着加字。
-
-微型测试验证的是**措辞**；对纪律执行类技能，它**不能替代**压力场景。
-
 **测试方法论：** 参见 @testing-skills-with-subagents.md 了解完整的测试方法：
 - 如何编写压力场景
 - 压力类型（时间、沉没成本、权威、疲惫）
@@ -628,7 +599,7 @@ helper1、helper2、step3、pattern4
 
 ## 技能创建清单（TDD 适配版）
 
-**重要：使用 TodoWrite 为下面的每个清单项创建待办。**
+**重要：使用当前平台的计划/待办工具跟踪下面的适用项。**
 
 **红色阶段 - 编写失败的测试：**
 - [ ] 创建压力场景（纪律类技能需 3 个以上组合压力）
@@ -661,9 +632,9 @@ helper1、helper2、step3、pattern4
 - [ ] 无叙事性故事
 - [ ] 支持文件仅用于工具或大量参考
 
-**部署：**
-- [ ] 将技能提交到 git 并推送到你的 fork（如果已配置）
-- [ ] 考虑通过 PR 贡献回去（如果具有广泛用途）
+**部署（仅在用户明确授权相应操作时）：**
+- [ ] 用户授权 commit 时，将技能提交到 Git
+- [ ] 用户授权 push/PR 时，再推送到 fork 或创建 PR
 
 ## 发现工作流
 
@@ -677,3 +648,12 @@ helper1、helper2、step3、pattern4
 
 **为此流程优化** - 把可搜索的术语放在前面和各处。
 
+## 总结
+
+**创建技能就是流程文档的 TDD。**
+
+同样的铁律：没有失败的测试就不写技能。
+同样的循环：红（基线）→ 绿（写技能）→ 重构（堵漏洞）。
+同样的好处：更高的质量、更少的意外、无懈可击的结果。
+
+如果你对代码遵循 TDD，对技能也应如此。这是同样的纪律应用于文档。
